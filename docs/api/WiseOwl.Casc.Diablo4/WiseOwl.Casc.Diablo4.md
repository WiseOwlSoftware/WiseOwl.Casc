# WiseOwl.Casc.Diablo4 assembly

## WiseOwl.Casc.Diablo4 namespace

| public type | description |
| --- | --- |
| record [AttributeFormula](./WiseOwl.Casc.Diablo4/AttributeFormula.md) | One named GameBalance attribute formula entry (`AttributeFormulaEntry`, stride 280). |
| class [AttributeFormulaTable](./WiseOwl.Casc.Diablo4/AttributeFormulaTable.md) | A decoded Diablo IV GameBalance `AttributeFormulas` table (`.gam`, SNO group 20; canonically SNO id 201912): the game's `name → formula text` table that paragon node/glyph magnitudes reference by GBID. Raw text + indices only — evaluation and the calibrated engine intrinsics are permanently the consumer's. |
| class [CombinedTextureMeta](./WiseOwl.Casc.Diablo4/CombinedTextureMeta.md) | The Diablo IV combined-meta bundle (magic `0x44CF00F5`, `Base\Texture-Base-Global.dat`): an indexed blob holding every [`TextureDefinition`](./WiseOwl.Casc.Diablo4/TextureDefinition.md). Texture metadata is not path-addressable per-SNO; it is consolidated here, while the pixel payloads remain addressable by SNO id (de-duplicated through the shared-payload mapping). |
| class [CoreToc](./WiseOwl.Casc.Diablo4/CoreToc.md) | Parser for Diablo IV's master SNO directory, `CoreTOC.dat`. Maps every `(group, id)` to a name and exposes the per-group payload format hash needed to dispatch SNO record parsing. |
| struct [DecodedImage](./WiseOwl.Casc.Diablo4/DecodedImage.md) | A decoded image: tightly-packed, row-major, straight-alpha RGBA32 (`R,G,B,A` bytes). Deliberately not tied to any imaging library — the consumer owns cropping/encoding/compositing (slice with [`PixelRect`](./WiseOwl.Casc.Diablo4/TexFrame/PixelRect.md)). |
| static class [Diablo4](./WiseOwl.Casc.Diablo4/Diablo4.md) | Diablo IV game-wide helpers that are not tied to an open storage. |
| class [Diablo4Storage](./WiseOwl.Casc.Diablo4/Diablo4Storage.md) | The Diablo IV game module: a thin, modern facade over a CascStorage that resolves content by SNO id through `CoreTOC.dat` and the TVFS file system, exposes the `0x44CF00F5` combined-meta texture catalog, and BLTE-decodes the result. The Diablo IV trademark appears only in this module, used nominatively as a compatibility descriptor. |
| struct [FormulaRange](./WiseOwl.Casc.Diablo4/FormulaRange.md) | One `arRanges` row of an [`AttributeFormula`](./WiseOwl.Casc.Diablo4/AttributeFormula.md): a power-range start, its two range values, and the formula source text. Raw decoded fields — the library never evaluates the text. |
| struct [NodeAttribute](./WiseOwl.Casc.Diablo4/NodeAttribute.md) | One attribute grant on a paragon node (an `AttributeSpecifier`, stride 88). Raw decoded fields only — the magnitude is produced by evaluating [`InlineFormula`](./WiseOwl.Casc.Diablo4/NodeAttribute/InlineFormula.md) or the GameBalance formula named by [`FormulaGbid`](./WiseOwl.Casc.Diablo4/NodeAttribute/FormulaGbid.md); evaluation and the calibrated intrinsics are the consumer's, not the library's (see `docs/casc-diablo4-format.md` Appendix C, library boundary). |
| class [ParagonBoardDefinition](./WiseOwl.Casc.Diablo4/ParagonBoardDefinition.md) | A decoded Diablo IV `ParagonBoardDefinition` (`.pbd`, SNO group 108): the square node grid for one paragon board. Raw fields only — no scoring, no evaluation (see the library boundary in `docs/casc-diablo4-format.md` Appendix C). |
| class [ParagonGlyphAffixDefinition](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixDefinition.md) | A decoded Diablo IV `ParagonGlyphAffixDefinition` (`.gaf`, SNO group 112). Raw fields only — magnitude interpretation (operation semantics, level scaling, thresholds) stays with the consumer. |
| class [ParagonGlyphDefinition](./WiseOwl.Casc.Diablo4/ParagonGlyphDefinition.md) | A decoded Diablo IV `ParagonGlyphDefinition` (`.gph`, SNO group 111). Raw fields only. |
| class [ParagonNodeDefinition](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition.md) | A decoded Diablo IV `ParagonNodeDefinition` (`.pgn`, SNO group 106). Raw fields only — no rarity scaling, no formula evaluation, no scoring (that interpretation is permanently the consumer's). |
| enum [ParagonRarity](./WiseOwl.Casc.Diablo4/ParagonRarity.md) | The verified first-party paragon node rarity values (`eRarityOverride`). A named-int convenience over [`RarityOverride`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/RarityOverride.md) — not policy; the raw int remains the serialized contract. |
| struct [SerialDataInfo](./WiseOwl.Casc.Diablo4/SerialDataInfo.md) | One mip's serialized-data locator (`SerialDataInfo`, 8 bytes) from a [`TextureDefinition`](./WiseOwl.Casc.Diablo4/TextureDefinition.md)'s `serTex` array. |
| class [SharedPayloadMapping](./WiseOwl.Casc.Diablo4/SharedPayloadMapping.md) | Diablo IV's `CoreTOCSharedPayloadsMapping.dat` (magic `0xABBA0003`): a payload de-duplication table. When a SNO's own payload section is empty, its pixel/blob bytes physically live under a different ("shared") SNO; this maps the requesting id → the id that actually holds the payload. Texture atlases (e.g. the per-class paragon node icons) rely on this. |
| struct [SnoEntry](./WiseOwl.Casc.Diablo4/SnoEntry.md) | One resolved `CoreTOC.dat` entry: a SNO record's group, numeric id, and name. |
| enum [SnoFolder](./WiseOwl.Casc.Diablo4/SnoFolder.md) | Which Diablo IV content partition a SNO is addressed in. A definition/record SNO lives in Meta; texture pixels live in Payload (with smaller mips in PayLow / PayMed). |
| enum [SnoGroup](./WiseOwl.Casc.Diablo4/SnoGroup.md) | Diablo IV SNO ("Sno", a typed game-object id namespace) group ids. Only the groups this module currently consumes are named; the full set has up to ~154 groups, all enumerable from [`CoreToc`](./WiseOwl.Casc.Diablo4/CoreToc.md). |
| class [StringListCatalog](./WiseOwl.Casc.Diablo4/StringListCatalog.md) | A parsed per-locale Diablo IV StringList catalog (`base/StringList-Text-<locale>.dat`): every localized `label → text` string in the game for one locale, grouped by table (SNO). This is genuinely a generic D4-container concern, so it lives in the library; consumers map a table by name/SNO then look up labels. |
| class [StringListTable](./WiseOwl.Casc.Diablo4/StringListTable.md) | One Diablo IV StringList table (a single `.stl` / SNO group 42 definition): an ordered set of `label → localized text` pairs. A table is a domain bucket — e.g. `AttributeDescriptions`, `Bnet_Chat`, a skill/affix/item table. |
| struct [TexFrame](./WiseOwl.Casc.Diablo4/TexFrame.md) | One atlas sub-rect (`TexFrame`, 36 bytes) from a [`TextureDefinition`](./WiseOwl.Casc.Diablo4/TextureDefinition.md)'s `ptFrame` array. |
| enum [TextureCodec](./WiseOwl.Casc.Diablo4/TextureCodec.md) | The pixel codec a [`Format`](./WiseOwl.Casc.Diablo4/TextureDefinition/Format.md) selects. |
| static class [TextureDecoder](./WiseOwl.Casc.Diablo4/TextureDecoder.md) | Clean-room block-compression (S3TC/DXT) decoder for the codecs Diablo IV UI atlases use. Implements BC1 (DXT1) and BC3 (DXT5) — the paragon node/glyph atlases are BC3. BC2/BC7/etc. are out of scope here (the catalog reports the codec via [`Codec`](./WiseOwl.Casc.Diablo4/TextureDefinition/Codec.md); callers can pick another decoder if needed). No external dependency. |
| record [TextureDefinition](./WiseOwl.Casc.Diablo4/TextureDefinition.md) | A parsed Diablo IV `TextureDefinition`: pixel format, base dimensions, the per-mip `serTex` table, and the atlas `ptFrame` sub-rects. The pixel payload itself is fetched separately (it is addressable by SNO id); this metadata only lives in the combined bundle (see [`CombinedTextureMeta`](./WiseOwl.Casc.Diablo4/CombinedTextureMeta.md)). |
| struct [UiField](./WiseOwl.Casc.Diablo4/UiField.md) | One bound field of a [`UiWidget`](./WiseOwl.Casc.Diablo4/UiWidget.md): the field-name hash (`= Diablo4.FieldHash(name)`), the underlying `DT_*` type hash (`= Diablo4.TypeHash("DT_…")`), and the raw 32-bit bound value (interpretation — int / SNO handle / RGBA / enum / byte — is the consumer's, per Type of the field). |
| record [UiScene](./WiseOwl.Casc.Diablo4/UiScene.md) | A decoded Diablo IV UI-scene SNO (CoreTOC group `46`, format hash `0xE4825AB8`) — the reflection / data-binding widget graph behind screens such as the paragon board. This is the raw graph only: per widget, its name, class id, and the schema + bound field values exactly as serialized. No evaluation, imaging, layout, or policy is applied (that boundary is the consumer's — see `docs/casc-diablo4-format.md` Appendix C and §10). For the typed paragon projection use `Diablo4Storage.ReadParagonRenderLayout` (§7.1 of the FR-C7 contract). |
| record [UiWidget](./WiseOwl.Casc.Diablo4/UiWidget.md) | One widget in a [`UiScene`](./WiseOwl.Casc.Diablo4/UiScene.md): its inline name, its class id (`= Diablo4.TypeHash(class name)`), and its bound fields in serialized order. |

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
