# WiseOwl.Casc.Diablo4 assembly

## WiseOwl.Casc.Diablo4 namespace

| public type | description |
| --- | --- |
| class [AffixDefinition](./WiseOwl.Casc.Diablo4/AffixDefinition.md) | A decoded Diablo IV `AffixDefinition` (`.aff`, SNO group Affix = 104) — an item/aspect/charm affix. Identity, localized [`Name`](./WiseOwl.Casc.Diablo4/AffixDefinition/Name.md)/[`Description`](./WiseOwl.Casc.Diablo4/AffixDefinition/Description.md), and the [`Effects`](./WiseOwl.Casc.Diablo4/AffixDefinition/Effects.md) — the attribute(s) the affix modifies. The rolled magnitude (min/max value range) and the additive-vs-multiplicative operation are not literal fields of the record for the bulk of stat affixes (they are item-power-curve driven by the engine); modeling those stays the consumer's domain per `casc-diablo4-format.md` Appendix C. |
| struct [AffixEffect](./WiseOwl.Casc.Diablo4/AffixEffect.md) | A single stat effect of an item/aspect [`AffixDefinition`](./WiseOwl.Casc.Diablo4/AffixDefinition.md) — the `(AttributeId, ParamPlus12)` pair identifying which attribute the affix modifies, plus the resolved localized [`AttributeName`](./WiseOwl.Casc.Diablo4/AffixEffect/AttributeName.md). One affix carries one [`AffixEffect`](./WiseOwl.Casc.Diablo4/AffixEffect.md) per modifier (a single-stat affix has one; a dual affix — e.g. a two-element resistance — has one per element). |
| struct [AnimSpec](./WiseOwl.Casc.Diablo4/AnimSpec.md) | An authored animation parameter set (raw; the consumer bakes one representative static frame). |
| struct [AssetFacets](./WiseOwl.Casc.Diablo4/AssetFacets.md) | FR-C20 P2 — cheap, decode-free facets of an asset, for filtering large kinds without fully decoding each. Fields are nullable: only those a kind can supply without a full decode are populated (today: texture-atlas dimensions / frame count / codec). Item/power/glyph categorical facets ride on [`Tags`](./WiseOwl.Casc.Diablo4/AssetRef/Tags.md) as they are derived. |
| enum [AssetKind](./WiseOwl.Casc.Diablo4/AssetKind.md) | FR-C20 — the family an [`AssetRef`](./WiseOwl.Casc.Diablo4/AssetRef.md) belongs to. Each kind is backed by one provider that knows how to enumerate its [`AssetRef`](./WiseOwl.Casc.Diablo4/AssetRef.md)s from `CoreTOC` and decode one into its strongly-typed definition. Kinds flagged as render recipes (the draw/composition surface) are also reachable via [`RenderRecipesOnly`](./WiseOwl.Casc.Diablo4/AssetQuery/RenderRecipesOnly.md).  Adding a new family is one new provider + one value here — no change to the [`Catalog`](./WiseOwl.Casc.Diablo4/Catalog.md) facade. Sub-classification within a kind (e.g. a weapon vs armor Item) rides on [`Tags`](./WiseOwl.Casc.Diablo4/AssetRef/Tags.md), not on new kinds. |
| struct [AssetLink](./WiseOwl.Casc.Diablo4/AssetLink.md) | FR-C20 P5 — one authored relationship edge from an asset to another (e.g. a board to one of its nodes, a node to its passive power, a glyph to an affix it grants). The [`Target`](./WiseOwl.Casc.Diablo4/AssetLink/Target.md) is a full [`AssetRef`](./WiseOwl.Casc.Diablo4/AssetRef.md), so traversal chains: [`Related`](./WiseOwl.Casc.Diablo4/Catalog/Related.md) the target again. Only authored FK edges are surfaced — runtime relationships (e.g. which glyph a player has socketed into a node) are not links. |
| record [AssetQuery](./WiseOwl.Casc.Diablo4/AssetQuery.md) | FR-C20 — a discovery filter for [`Find`](./WiseOwl.Casc.Diablo4/Catalog/Find.md). All set members are combined with `AND`; an unset (null/false) member does not filter. |
| struct [AssetRef](./WiseOwl.Casc.Diablo4/AssetRef.md) | FR-C20 — a lightweight, decode-free handle to one catalogued asset: its kind, SNO identity, name, and classification tags. Returned by [`Find`](./WiseOwl.Casc.Diablo4/Catalog/Find.md); pass to [`TryGet`](./WiseOwl.Casc.Diablo4/Catalog/TryGet.md) (or [`TryGet`](./WiseOwl.Casc.Diablo4/Catalog/TryGet.md)) to decode it. |
| record [AttributeFormula](./WiseOwl.Casc.Diablo4/AttributeFormula.md) | One named GameBalance attribute formula entry (`AttributeFormulaEntry`, stride 280). |
| class [AttributeFormulaTable](./WiseOwl.Casc.Diablo4/AttributeFormulaTable.md) | A decoded Diablo IV GameBalance `AttributeFormulas` table (`.gam`, SNO group 20; canonically SNO id 201912): the game's `name → formula text` table that paragon node/glyph magnitudes reference by GBID. Raw text + indices only — evaluation and the calibrated engine intrinsics are permanently the consumer's. |
| static class [AttributeNames](./WiseOwl.Casc.Diablo4/AttributeNames.md) | FR-C25 — resolve a Diablo IV `AttributeId` (the raw `eAttribute` int on a [`NodeAttribute`](./WiseOwl.Casc.Diablo4/NodeAttribute.md) / [`ParagonGlyphAffixDefinition`](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixDefinition.md)) to its in-game localized name via the engine's `AttributeDescriptions` StringList (sno `4080`) — the same source the tooltip renderer uses. |
| struct [CanvasRef](./WiseOwl.Casc.Diablo4/CanvasRef.md) | The UI design space the raw rects are authored in (decoded from the root `ParagonBoard_main` widget; verified 1920×1200). |
| class [Catalog](./WiseOwl.Casc.Diablo4/Catalog.md) | FR-C20 — the asset discovery/retrieval facade ([`Catalog`](./WiseOwl.Casc.Diablo4/Diablo4Storage/Catalog.md)). Lets a consumer discover what the game contains and retrieve the decoded definition without hardcoding SNO ids/names or knowing which typed reader to call: `Find` a filtered set of [`AssetRef`](./WiseOwl.Casc.Diablo4/AssetRef.md)s, then `TryGet` the decoded value (pattern-match the real type, or ask for it via [`TryGet`](./WiseOwl.Casc.Diablo4/Catalog/TryGet.md)). The strongly-typed accessors on [`Diablo4Storage`](./WiseOwl.Casc.Diablo4/Diablo4Storage.md) remain as ergonomic shortcuts over the same providers. |
| record [CharacterClass](./WiseOwl.Casc.Diablo4/CharacterClass.md) | One Diablo IV playable character class, decoded first-party from the game's own class-definition data (FR-D2). A class is a first-class D4 concept independent of paragon; this type carries only the raw decoded roster identity + localized name — no policy, no imaging. |
| static class [CharacterStatModel](./WiseOwl.Casc.Diablo4/CharacterStatModel.md) | Universal (class-independent) constants for the Diablo IV Character-Sheet stat model (FR-C29). The per-point core→stat conversion coefficients and the inherent base stats were not located in any searched SNO source (a thorough data-mine across every candidate GameBalance/class source found no coefficient home — see `docs/casc-diablo4-format.md §12`): they are either engine-side or in a global config not yet identified. They are universal — identical for every class, so a newly-added class reuses them and supplies only its own data-driven core→stat map (which is why they need not be per-class data). They are baked here as owner-oracle-validated constants (the engine-constants pattern; precedent CL-68 / CL-83), pinned against live core-stat tooltips for four classes spanning all four primary-attribute archetypes (Warlock, Rogue, Necromancer, Barbarian), including a high-Paragon capture that fixed the small-magnitude rates to three significant figures. |
| class [CombinedTextureMeta](./WiseOwl.Casc.Diablo4/CombinedTextureMeta.md) | The Diablo IV combined-meta bundle (magic `0x44CF00F5`, `Base\Texture-Base-Global.dat`): an indexed blob holding every [`TextureDefinition`](./WiseOwl.Casc.Diablo4/TextureDefinition.md). Texture metadata is not path-addressable per-SNO; it is consolidated here, while the pixel payloads remain addressable by SNO id (de-duplicated through the shared-payload mapping). |
| enum [ConversionUnit](./WiseOwl.Casc.Diablo4/ConversionUnit.md) | The unit of a [`PerPoint`](./WiseOwl.Casc.Diablo4/CoreStatConversion/PerPoint.md) coefficient. |
| enum [CoreStat](./WiseOwl.Casc.Diablo4/CoreStat.md) | A Diablo IV core attribute — the four primary stats every character has. The integer value is the game's internal core-attribute index (the value stored in the [`PlayerClassDefinition`](./WiseOwl.Casc.Diablo4/PlayerClassDefinition.md) stat-conversion arrays). |
| record [CoreStatConversion](./WiseOwl.Casc.Diablo4/CoreStatConversion.md) | One core-attribute → derived-stat conversion: how many units of [`Stat`](./WiseOwl.Casc.Diablo4/CoreStatConversion/Stat.md) each point of [`Core`](./WiseOwl.Casc.Diablo4/CoreStatConversion/Core.md) grants. The coefficient is a universal engine constant (identical for every class — only the core→stat mapping is per-class). |
| class [CoreToc](./WiseOwl.Casc.Diablo4/CoreToc.md) | Parser for Diablo IV's master SNO directory, `CoreTOC.dat`. Maps every `(group, id)` to a name and exposes the per-group payload format hash needed to dispatch SNO record parsing. |
| struct [DecodedImage](./WiseOwl.Casc.Diablo4/DecodedImage.md) | A decoded image: tightly-packed, row-major, straight-alpha RGBA32 (`R,G,B,A` bytes). Deliberately not tied to any imaging library — the consumer owns cropping/encoding/compositing (slice with [`PixelRect`](./WiseOwl.Casc.Diablo4/TexFrame/PixelRect.md)). |
| enum [DerivedStat](./WiseOwl.Casc.Diablo4/DerivedStat.md) | A Character-Sheet stat that a [`CoreStat`](./WiseOwl.Casc.Diablo4/CoreStat.md) contributes to via a per-point conversion. The four "signature" stats (Armor/ResistanceAllElements/HealingReceived/DodgeChance) are bound to a fixed core for every class; the three "mobile" stats (SkillDamage/CriticalStrikeChance/ResourceGeneration) are bound to a per-class core named by the class record (see [`StatConversions`](./WiseOwl.Casc.Diablo4/PlayerClassDefinition/StatConversions.md)). |
| static class [Diablo4](./WiseOwl.Casc.Diablo4/Diablo4.md) | Diablo IV game-wide helpers that are not tied to an open storage. |
| class [Diablo4Storage](./WiseOwl.Casc.Diablo4/Diablo4Storage.md) | The Diablo IV game module: a thin, modern facade over a CascStorage that resolves content by SNO id through `CoreTOC.dat` and the TVFS file system, exposes the `0x44CF00F5` combined-meta texture catalog, and BLTE-decodes the result. The Diablo IV trademark appears only in this module, used nominatively as a compatibility descriptor. |
| class [DifficultyTierRow](./WiseOwl.Casc.Diablo4/DifficultyTierRow.md) | One [`DifficultyTiersTable`](./WiseOwl.Casc.Diablo4/DifficultyTiersTable.md) row — a monster level's scaling coefficients (FR-C34, CL-101). Carries the verified/inferred typed columns plus the full raw column vector for the unlabeled coefficients. |
| class [DifficultyTiersTable](./WiseOwl.Casc.Diablo4/DifficultyTiersTable.md) | A decoded Diablo IV `DifficultyTiers` GameBalance table (SNO 1973217, group 20): the per-monster-level scaling curve — the monster/content analogue of the player-side [`LevelScalingTable`](./WiseOwl.Casc.Diablo4/LevelScalingTable.md) (FR-C34, CL-101). 150 rows, monster levels `1..150`, row index = `level − 1`. |
| struct [Facet](./WiseOwl.Casc.Diablo4/Facet.md) | FR-C20 P2b — one categorical facet of an asset (e.g. `class=Barbarian`), with its [`Source`](./WiseOwl.Casc.Diablo4/Facet/Source.md) provenance. Use for discovery/filtering (see [`Facets`](./WiseOwl.Casc.Diablo4/Catalog/Facets.md) / [`FindByFacet`](./WiseOwl.Casc.Diablo4/Catalog/FindByFacet.md)); an asset may carry several of the same [`Key`](./WiseOwl.Casc.Diablo4/Facet/Key.md) (e.g. a glyph usable by multiple classes). |
| enum [FacetSource](./WiseOwl.Casc.Diablo4/FacetSource.md) | FR-C20 P2b — where a categorical [`Facet`](./WiseOwl.Casc.Diablo4/Facet.md)'s value came from, so the consumer knows how much to trust it (mirrors `NodeActivationSource`). Discovery/filtering may use any source; authored rendering must not lean on NameConvention. |
| struct [FormulaRange](./WiseOwl.Casc.Diablo4/FormulaRange.md) | One `arRanges` row of an [`AttributeFormula`](./WiseOwl.Casc.Diablo4/AttributeFormula.md): the item-power band this row applies to, its output clamps, and the formula source text. Raw decoded fields — the library never evaluates the text. |
| struct [GlyphAffixAttributeRef](./WiseOwl.Casc.Diablo4/GlyphAffixAttributeRef.md) | A single `(AttributeId, ParamPlus12)` entry from a glyph affix's [`AffectedAttributes`](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixDefinition/AffectedAttributes.md) list. The shape parallels the `ptAttributes` pair carried on a [`ParagonNodeDefinition`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition.md) — an `eAttribute` int and a `uParamPlus12` GBID — but the glyph-affix encoding is the trimmed 8-byte pair only (no inline formula, no parallel attribute-GBID, no `NParam`), so it is surfaced as a distinct record struct rather than reusing [`NodeAttribute`](./WiseOwl.Casc.Diablo4/NodeAttribute.md)'s 88-byte node-attribute shape. |
| enum [ItemClass](./WiseOwl.Casc.Diablo4/ItemClass.md) | The broad equipment category of an [`ItemType`](./WiseOwl.Casc.Diablo4/ItemType.md) — the top-level grouping a gear/item API filters on (LIB-1). Derived structurally from the item base-type record, not from its name. |
| class [ItemDefinition](./WiseOwl.Casc.Diablo4/ItemDefinition.md) | A decoded Diablo IV `ItemDefinition` (`.itm`, SNO group Item = 73) — an item record. Identity + localized text only; item stat/affix/power modeling stays the consumer's domain spec (Appendix C). |
| class [ItemType](./WiseOwl.Casc.Diablo4/ItemType.md) | A decoded Diablo IV item base type — an entry in the item-type dictionary (SNO group ItemType = 98, the game's `eItemType` enum: `Sword`, `Amulet`, `Helm`, `Charm`, `HealthPotion`, …). Every [`ItemDefinition`](./WiseOwl.Casc.Diablo4/ItemDefinition.md) references its base type ([`ItemTypeSnoId`](./WiseOwl.Casc.Diablo4/ItemDefinition/ItemTypeSnoId.md)); this record is what classifies an item as a weapon / armor / jewelry / charm (LIB-1). |
| class [LevelScalingRow](./WiseOwl.Casc.Diablo4/LevelScalingRow.md) | One [`LevelScalingTable`](./WiseOwl.Casc.Diablo4/LevelScalingTable.md) row — a level's scaling coefficients (CL-102). Carries the verified [`HpScalar`](./WiseOwl.Casc.Diablo4/LevelScalingRow/HpScalar.md) plus the full raw column vector for the remaining, unlabeled columns. |
| class [LevelScalingTable](./WiseOwl.Casc.Diablo4/LevelScalingTable.md) | A decoded Diablo IV `LevelScaling` GameBalance table (SNO 206158, group 20): the per-level scaling curve that drives base Max Life (and monster scaling). Indexed `1..200`; character levels occupy `1..70` ([`MaxCharacterLevel`](./WiseOwl.Casc.Diablo4/LevelScalingTable/MaxCharacterLevel.md) — the cap on build `3.1.1.72836`), while `71..200` are monster / content levels — one [`HpScalar`](./WiseOwl.Casc.Diablo4/LevelScalingTable/HpScalar.md) column serves both populations. |
| struct [MonsterNameFragment](./WiseOwl.Casc.Diablo4/MonsterNameFragment.md) | One entry of the Diablo IV `MonsterNames` registry (FR-C35, CL-105) — a localized name fragment the game composes into an elite/special monster's displayed name (e.g. `FrozenSuffix004` → `"Frostburn"`, `ElectricLanceSuffix001` → `"Boltrend"`). |
| enum [MonsterNameKind](./WiseOwl.Casc.Diablo4/MonsterNameKind.md) | The kind of a [`MonsterNameFragment`](./WiseOwl.Casc.Diablo4/MonsterNameFragment.md) — where the fragment sits in a composed elite-monster name (FR-C35, CL-105). |
| class [MonsterNameRegistry](./WiseOwl.Casc.Diablo4/MonsterNameRegistry.md) | The decoded Diablo IV `MonsterNames` registry (FR-C35, CL-105): the localized name-affix fragments the game composes into elite/special monster display names. |
| record [NodeActivation](./WiseOwl.Casc.Diablo4/NodeActivation.md) | FR-C16 R11 — the typed activation condition of a recipe layer: the engine-sourced predicate over [`NodeFact`](./WiseOwl.Casc.Diablo4/NodeFact.md) that gates whether the layer draws. The consumer supplies the set of currently-true facts and calls [`Evaluate`](./WiseOwl.Casc.Diablo4/NodeActivation/Evaluate.md); it authors no predicate of its own. |
| enum [NodeActivationSource](./WiseOwl.Casc.Diablo4/NodeActivationSource.md) | FR-C16 R11 — where a [`NodeActivation`](./WiseOwl.Casc.Diablo4/NodeActivation.md)'s condition was recovered from. The honesty marker for the `feedback_widget-name-not-role` discipline: a `SceneField` activation is read verbatim from the data; a `NameConvention` one is decoded from the engine's own state-suffixed identifier; an `EngineBehavior` one is CASC's documented inference where the name is suggestive but not literal. |
| struct [NodeAttribute](./WiseOwl.Casc.Diablo4/NodeAttribute.md) | One attribute grant on a paragon node (an `AttributeSpecifier`, stride 88). Raw decoded fields only — the magnitude is produced by evaluating [`InlineFormula`](./WiseOwl.Casc.Diablo4/NodeAttribute/InlineFormula.md) or the GameBalance formula named by [`FormulaGbid`](./WiseOwl.Casc.Diablo4/NodeAttribute/FormulaGbid.md); evaluation and the calibrated intrinsics are the consumer's, not the library's (see `docs/casc-diablo4-format.md` Appendix C, library boundary). |
| struct [NodeElement](./WiseOwl.Casc.Diablo4/NodeElement.md) | A drawable element of the paragon node composite recipe: its raw texture handle (`== TexFrame.ImageHandle`; `0xFFFFFFFF`/0 ⇒ none, never pre-resolved), the atlas SNO that hosts the frame, the frame's native pixel size (the layer's authoritative draw extent when no widget rect is authored — the engine draws at native px size centred on the disc anchor), the authored reference-unit rect (`default` ⇒ inherits `NodeTemplate` at native px), and raw `dwAlpha`. |
| enum [NodeFact](./WiseOwl.Casc.Diablo4/NodeFact.md) | FR-C16 R11 — the closed vocabulary of runtime node facts a consumer computes and feeds to a layer's [`NodeActivation`](./WiseOwl.Casc.Diablo4/NodeActivation.md). Each value is a single boolean condition the engine's UI controller keys a widget's visibility off. The consumer owns ONLY the computation of these facts (genuine app state — which node is selected, purchased, reachable, …); the mapping fact→layer is the engine-sourced [`NodeActivation`](./WiseOwl.Casc.Diablo4/NodeActivation.md), never consumer-authored. |
| struct [NodeSelectionHighlight](./WiseOwl.Casc.Diablo4/NodeSelectionHighlight.md) | FR-C19 — the paragon node mouse-over (hover) selection highlight, as the engine authors it: the `ContextualHighlight_Square` recipe (a 4-piece `TiledWindowPieces` TiledStyle — the named "square contextual highlight") paired with its drawable corner art (the 4 corner frames of the `2DUITiled_SelectionHighlight` atlas, surfaced via `SelectionRectangleInset`'s window-pieces). Drawing recipe (authored, owner-validated): a hollow square border sized to the node's bounding square. Draw only the 4 corners — each piece is a full quadrant (corner + both half-edges), so the four meet to surround the node; place [`TopLeft`](./WiseOwl.Casc.Diablo4/NodeSelectionHighlight/TopLeft.md) in the top-left quadrant, [`TopRight`](./WiseOwl.Casc.Diablo4/NodeSelectionHighlight/TopRight.md) top-right, etc. No edge or centre pieces, no fill (the node shows through). Resolve each corner handle via the texture path ([`TryGetIconFrame`](./WiseOwl.Casc.Diablo4/Diablo4Storage/TryGetIconFrame.md) → decode) for the orange-glow + white-edge art. |
| record [ParagonBoardChrome](./WiseOwl.Casc.Diablo4/ParagonBoardChrome.md) | The paragon board chrome render model (§10.16). The main board (scene 657304) composes a 5-piece chrome: a centre background field ([`BackgroundCenter`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BackgroundCenter.md)) plus a 4-cardinal-side rim — [`BorderTop`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BorderTop.md) / [`BorderBottom`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BorderBottom.md) share one band texture, [`BorderLeft`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BorderLeft.md) / [`BorderRight`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BorderRight.md) share another. [`BoardSelectChrome`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BoardSelectChrome.md) carries the board-select panel's preview frame + filigree band from scene 964599. The centre field ([`BackgroundCenter`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BackgroundCenter.md)) carries an authored `1200×1200` reference-unit rect (decoded FR-C16 R7 — its `nWidth`/`nHeight` are tag-2-encoded; pre-R7 the 0x22-only parser read no records for this widget and so reported an all-zero rect — an artifact, now corrected). The 4 rim sides bind no `nWidth`/`nHeight` at all, so they remain engine-positioned at native pixel size (their rect stays zero, faithfully). The rim-band handles ([`BorderTop`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/BorderTop.md)'s, etc.) are scene-bound via the standard `0x6B1C5D9C` texture-handle field but resolve through a non-icon-catalog path CASC does not currently index, so their [`AtlasSno`](./WiseOwl.Casc.Diablo4/NodeElement/AtlasSno.md) / [`NativeWidth`](./WiseOwl.Casc.Diablo4/NodeElement/NativeWidth.md) / [`NativeHeight`](./WiseOwl.Casc.Diablo4/NodeElement/NativeHeight.md) are `0` (consumer uses a different texture-resolution path or a procedural equivalent). Any rim animation (the engine-animated "fire" the game shows on the rim) is **engine-internal** — scene data has no blend mode, frame order, or timing for it; per CL-28 / CL-30 no-fabrication discipline CASC does not surface a fabricated sequence. |
| class [ParagonBoardDefinition](./WiseOwl.Casc.Diablo4/ParagonBoardDefinition.md) | A decoded Diablo IV `ParagonBoardDefinition` (`.pbd`, SNO group 108): the square node grid for one paragon board. Raw fields only — no scoring, no evaluation (see the library boundary in `docs/casc-diablo4-format.md` Appendix C). |
| record [ParagonBoardGrid](./WiseOwl.Casc.Diablo4/ParagonBoardGrid.md) | FR-C17 — the engine's paragon-board grid-layout metric, in the authored design-canvas reference units. The consumer maps a board cell's grid coordinate to a canvas position with `canvasPos = (gridX·Pitch, gridY·Pitch)` (relative to the grid origin) and scales the whole canvas `CanvasWidth×CanvasHeight → render resolution`. This replaces the consumer's empirical pitch (the FR-C7-measured ~67.7px / arbitrary CellPx) with the engine's authored metric.  All values are read from the live UI scene (game data), not hard-coded: [`CanvasWidth`](./WiseOwl.Casc.Diablo4/ParagonBoardGrid/CanvasWidth.md)/[`CanvasHeight`](./WiseOwl.Casc.Diablo4/ParagonBoardGrid/CanvasHeight.md) from the root `ParagonBoard_main` widget, [`CellExtent`](./WiseOwl.Casc.Diablo4/ParagonBoardGrid/CellExtent.md) from the `Template_Node_Common` node-cell widget. The board's logical grid (dimensions + which cell holds which node) is the per-board [`ParagonBoardDefinition`](./WiseOwl.Casc.Diablo4/ParagonBoardDefinition.md) (`Width` + `Cells`); this record is the global canvas/cell metric shared across boards. Pitch = CellExtent (cells are laid out adjacent, no extra inter-cell gap beyond the per-cell art inset). Validated against the owner's in-game measurement: the empirical ~67.7px pitch = `CellExtent (100) × render-scale (≈0.677 at the consumer's board width)`, so the authored 100-unit cell reproduces the observed spacing exactly. If a future build introduces an explicit inter-cell gap it would surface as `Pitch ≠ CellExtent` (engine render-code RE — the `UIParagonBoardStyle` class is a style wrapper with no grid-layout fields, so any gap lives in the engine's grid-layout code, not the scene data). |
| record [ParagonBoundWidget](./WiseOwl.Casc.Diablo4/ParagonBoundWidget.md) | A scene widget that binds ≥1 real atlas texture handle: its name, class id, and the bound layers (handle + decoded rect + alpha) regardless of binding shape (0x22 field or the 0x58 block). Raw + complete; the consumer assigns role/state. |
| class [ParagonGlyphAffixDefinition](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixDefinition.md) | A decoded Diablo IV `ParagonGlyphAffixDefinition` (`.gaf`, SNO group 112). Raw fields plus the projected slice (formula identity, affected-attribute pairs, tag GBID list, linked-power ref); magnitude interpretation (operation semantics, level scaling, the per-class threshold gate, legendary unlock level) stays with the consumer per the durable library boundary in `casc-diablo4-format.md` Appendix C. |
| enum [ParagonGlyphAffixOperation](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixOperation.md) | The kind of bonus mechanism a [`ParagonGlyphAffixDefinition`](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixDefinition.md) applies — the typed view of the raw `eBonusOperation` int ([`Operation`](./WiseOwl.Casc.Diablo4/ParagonGlyphAffixDefinition/Operation.md)) at payload `+0x30`. Values match the live `3.0.2.71886` build. |
| class [ParagonGlyphDefinition](./WiseOwl.Casc.Diablo4/ParagonGlyphDefinition.md) | A decoded Diablo IV `ParagonGlyphDefinition` (`.gph`, SNO group 111). Raw fields only. |
| struct [ParagonGridCell](./WiseOwl.Casc.Diablo4/ParagonGridCell.md) | A grid coordinate on a paragon board's authored layout — row-major from the top-left (row `0` = top, col `0` = left). Paired with a [`ParagonNodeInfo`](./WiseOwl.Casc.Diablo4/ParagonNodeInfo.md) by `Catalog.GetBoardNodes(int)` to describe placement on the board. |
| static class [ParagonMagnitudeFormula](./WiseOwl.Casc.Diablo4/ParagonMagnitudeFormula.md) | Evaluate a paragon stat-node magnitude formula text to its displayed numeric value (FR-C21). The grammar is a strict subset of the engine's formula DSL: a numeric literal, a zero-arg call to one of the six budget-multiplier intrinsics from [`ParagonPowerBudget`](./WiseOwl.Casc.Diablo4/ParagonPowerBudget.md), binary `+ - * /`, and parentheses. |
| record [ParagonNodeComponent](./WiseOwl.Casc.Diablo4/ParagonNodeComponent.md) | FR-C16 R14 — one atomic drawable component of a [`ParagonNodeRecipe`](./WiseOwl.Casc.Diablo4/ParagonNodeRecipe.md): a single atlas frame drawn at its rect/alpha when its [`Activation`](./WiseOwl.Casc.Diablo4/ParagonNodeComponent/Activation.md) holds. The flattening of the former layer/disc/composite nesting — every per-rarity disc, per-state ornate, interior fill, glow, arrow, and symbol slot is its own component with the exact combined condition. |
| class [ParagonNodeDefinition](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition.md) | A decoded Diablo IV `ParagonNodeDefinition` (`.pgn`, SNO group 106). Raw fields only — no rarity scaling, no formula evaluation, no scoring (that interpretation is permanently the consumer's). |
| record [ParagonNodeInfo](./WiseOwl.Casc.Diablo4/ParagonNodeInfo.md) | FR-C21 — the display-ready projection of one paragon node. The library evaluates magnitudes + infers units + resolves names so the consumer can render the in-game tooltip without re-walking the raw byte fields or owning the formula evaluator (per the Appendix C carve-out for this surface). |
| enum [ParagonNodeKind](./WiseOwl.Casc.Diablo4/ParagonNodeKind.md) | The display-axis classification of a paragon node — what the in-game tooltip headline shows (the visual archetype, distinct from [`ParagonRarity`](./WiseOwl.Casc.Diablo4/ParagonRarity.md)). Derived from [`NodeType`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/NodeType.md) + [`IsGate`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/IsGate.md) + [`HasSocket`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/HasSocket.md) + [`Rarity`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/Rarity.md). |
| record [ParagonNodeRecipe](./WiseOwl.Casc.Diablo4/ParagonNodeRecipe.md) | FR-C16 R14 — the engine's per-node render PROGRAM as a flat, truly-z-ordered list of atomic components. The consumer is a pure interpreter with one rule: |
| record [ParagonNodeStat](./WiseOwl.Casc.Diablo4/ParagonNodeStat.md) | One stat grant on a paragon node — the display-ready projection of a [`NodeAttribute`](./WiseOwl.Casc.Diablo4/NodeAttribute.md). The library does the formula evaluation and unit inference for the node-info surface (FR-C21 carve-out from the Appendix C boundary). |
| enum [ParagonNodeType](./WiseOwl.Casc.Diablo4/ParagonNodeType.md) | The verified first-party paragon node `eNodeType` values ([`NodeType`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/NodeType.md), payload `+16`). A named-int convenience over the raw int — not policy; the raw int remains the serialized contract. Observed across all seven class boards: every class start node is Start (5); magic nodes are Magic (3); normal/structural/gate/rare nodes are Normal (0). This is a distinct axis from [`ParagonRarity`](./WiseOwl.Casc.Diablo4/ParagonRarity.md) (e.g. a rare node is `NodeType 0` with `RarityOverride 3`); values other than 0/3/5 have not been observed and would surface here as the raw cast. |
| static class [ParagonPowerBudget](./WiseOwl.Casc.Diablo4/ParagonPowerBudget.md) | The six budget-multiplier intrinsics that paragon-node magnitude formulas multiply against. Empirically pinned (clean-room) from owner-validated in-game readings on build `3.0.2.71886`; the engine implements these as formula-DSL intrinsic functions (`ParagonPowerBudgetMultiplierNode<Rarity>{Major|Minor}<Off|Def>()`) that are absent from every shipped GameBalance data table — not in [`AttributeFormulaTable`](./WiseOwl.Casc.Diablo4/AttributeFormulaTable.md) (sno `201912`), not in any `PowerFormulaTables`, and no SNO carries "Budget" in its name. So the library bakes them as a calibration table. |
| enum [ParagonRarity](./WiseOwl.Casc.Diablo4/ParagonRarity.md) | The verified first-party paragon node rarity values (`eRarityOverride`). A named-int convenience over [`RarityOverride`](./WiseOwl.Casc.Diablo4/ParagonNodeDefinition/RarityOverride.md) — not policy; the raw int remains the serialized contract. |
| record [ParagonRenderLayout](./WiseOwl.Casc.Diablo4/ParagonRenderLayout.md) | The typed paragon-board render projection (FR-C7), built on top of the generic [`UiScene`](./WiseOwl.Casc.Diablo4/UiScene.md) decode of `ParagonBoard` (SNO 657304). Raw decoded geometry only — the consumer owns the resolution/zoom scale and all imaging/compositing (boundary: `docs/casc-diablo4-format.md` Appendix C, §10.7). |
| record [ParagonRenderModel](./WiseOwl.Casc.Diablo4/ParagonRenderModel.md) | The exhaustive paragon render-model (FR-C9): the role-assigned [`Layout`](./WiseOwl.Casc.Diablo4/ParagonRenderModel/Layout.md) (FR-C7/C8 typed projection) plus, for every paragon UI-scene, every widget that binds at least one real atlas texture handle — with the handle, its decoded [`WidgetRect`](./WiseOwl.Casc.Diablo4/WidgetRect.md), and alpha. This is the one-shot audit surface: the library guarantees it is complete (no binding shape dropped — proven by the FR-C9 coverage gate); the consumer owns role/state classification (FR-C7 §6 boundary). |
| record [ParagonSceneModel](./WiseOwl.Casc.Diablo4/ParagonSceneModel.md) | One paragon UI-scene's complete atlas-binding model: every widget that binds ≥1 real atlas handle. |
| record [ParagonTooltipChrome](./WiseOwl.Casc.Diablo4/ParagonTooltipChrome.md) | FR-C23 (Option A) — the engine's authored paragon-node tooltip chrome surfaced as a typed asset-reference recipe. The consumer looks up the entry for a node's [`ParagonRarity`](./WiseOwl.Casc.Diablo4/ParagonRarity.md) and composites the panel via the existing `Diablo4Storage.ReadTiledStyle` / `Catalog.TryGet<TiledStyleDefinition>` path (the returned [`AssetRef`](./WiseOwl.Casc.Diablo4/AssetRef.md)s are the same shape as everything else [`Catalog`](./WiseOwl.Casc.Diablo4/Catalog.md) hands out). |
| class [PlayerClassDefinition](./WiseOwl.Casc.Diablo4/PlayerClassDefinition.md) | A decoded Diablo IV `PlayerClassDefinition` (`.prd`, SNO group PlayerClass = 74) — the playable character class record. Exposes identity ([`SnoId`](./WiseOwl.Casc.Diablo4/PlayerClassDefinition/SnoId.md)/[`EClass`](./WiseOwl.Casc.Diablo4/PlayerClassDefinition/EClass.md)) plus the per-class Character-Sheet stat-conversion map (FR-C29): which core attribute feeds each derived stat. (The class roster + localized names are [`ReadCharacterClasses`](./WiseOwl.Casc.Diablo4/Diablo4Storage/ReadCharacterClasses.md).) |
| class [PowerDefinition](./WiseOwl.Casc.Diablo4/PowerDefinition.md) | A decoded Diablo IV `PowerDefinition` (`.pow`, SNO group Power = 29) — a skill/passive/legendary power. Identity + localized text + the structured Script Formula slot table (FR-C13 Phase 1); the engine-internal AST evaluator that resolves non-trivial SF_N expressions to runtime values is the consumer's stat-effect model per the library boundary (Appendix C). FR-C13 Phase 2 (forthcoming) lifts the trivial-numeric resolution into the library; until then, expression-typed slots ([`IsExpression`](./WiseOwl.Casc.Diablo4/PowerScriptFormula/IsExpression.md)) carry their raw text form (e.g. `"SF_1 / 3"`) for consumer-side evaluation. |
| struct [PowerFunctionRef](./WiseOwl.Casc.Diablo4/PowerFunctionRef.md) | FR-C13 Phase 2 — one engine-function reference that appears in a Power's `DT_STRING_FORMULA` text expressions or in its localized [`Description`](./WiseOwl.Casc.Diablo4/PowerDefinition/Description.md) format string. The library surfaces the reference structurally (name + resolved arg values); the consumer registers a resolver delegate keyed by [`Name`](./WiseOwl.Casc.Diablo4/PowerFunctionRef/Name.md) to substitute the engine value at runtime (engine-runtime player-state is outside CASC's domain). |
| struct [PowerModifier](./WiseOwl.Casc.Diablo4/PowerModifier.md) | LIB-5 (CL-104) — one selectable modifier of a skill ([`Modifiers`](./WiseOwl.Casc.Diablo4/PowerDefinition/Modifiers.md)): a skill-tree enhancement / upgrade node with its localized display name + full effect description. |
| struct [PowerScriptFormula](./WiseOwl.Casc.Diablo4/PowerScriptFormula.md) | FR-C13 Phase 1 — one slot of a Power's Script Formula table. Each slot is a positional, named entry the engine resolves through its runtime formula evaluator; the library surfaces the entry's decoded text form + literal IEEE-754 value (when the text is a numeric literal) so the consumer can evaluate the SF_N references in [`Description`](./WiseOwl.Casc.Diablo4/PowerDefinition/Description.md)'s format string. |
| struct [RenderRatios](./WiseOwl.Casc.Diablo4/RenderRatios.md) | Library-derived unitless render ratios (the primary consume path; C-c). All are fractions, never pixels — the consumer applies its own resolution/zoom basis (permanently consumer-owned). |
| struct [RgbaTint](./WiseOwl.Casc.Diablo4/RgbaTint.md) | A raw bound `DT_RGBACOLOR` value. |
| record [SelectionHighlight](./WiseOwl.Casc.Diablo4/SelectionHighlight.md) | FR-C19 — the engine's mouse-over / cursor selection highlight, surfaced as the set of authored recipes that draw it. The selection highlight is orthogonal to, and not part of, the paragon node render recipe ([`ParagonNodeRecipe`](./WiseOwl.Casc.Diablo4/ParagonNodeRecipe.md)): a node can be selected whether or not it is purchased, so the consumer composes the node from its recipe and then, if the node is selected, draws the matching selection style on top.  Crucially, the game does not require the consumer to assemble the highlight from loose atlas frames — it authors each selection shape as a [`TiledStyle`](./WiseOwl.Casc.Diablo4/TiledStyleDefinition.md) 9-slice recipe (group UiStyle = 103) over the `2DUI_SelectionHighlight` / `2DUITiled_SelectionHighlight` atlases. This accessor returns those authored recipes ([`Styles`](./WiseOwl.Casc.Diablo4/SelectionHighlight/Styles.md)) so the consumer applies the existing TiledStyle machinery ([`ReadTiledStyle`](./WiseOwl.Casc.Diablo4/Diablo4Storage/ReadTiledStyle.md)) — exactly the path used for every other tiled UI drawing — and performs no composition or shape-classification of its own.  Each style carries its authored name and the [`SelectionShape`](./WiseOwl.Casc.Diablo4/SelectionShape.md) derived from it (e.g. `ControllerSelectionCircle` → Circle, `SelectionRectangleInset` → Rectangle): the consumer selects the style whose shape matches the node's silhouette. The shape comes from the engine's own recipe name, not from an atlas-frame-size guess. |
| struct [SelectionHighlightStyle](./WiseOwl.Casc.Diablo4/SelectionHighlightStyle.md) | FR-C19 — one authored selection-highlight recipe: a TiledStyle (group UiStyle) that 9-slices a selection atlas. |
| enum [SelectionShape](./WiseOwl.Casc.Diablo4/SelectionShape.md) | FR-C19 — node silhouette a [`SelectionHighlightStyle`](./WiseOwl.Casc.Diablo4/SelectionHighlightStyle.md) frames, classified from the authored TiledStyle name. |
| struct [SerialDataInfo](./WiseOwl.Casc.Diablo4/SerialDataInfo.md) | One mip's serialized-data locator (`SerialDataInfo`, 8 bytes) from a [`TextureDefinition`](./WiseOwl.Casc.Diablo4/TextureDefinition.md)'s `serTex` array. |
| class [SharedPayloadMapping](./WiseOwl.Casc.Diablo4/SharedPayloadMapping.md) | Diablo IV's `CoreTOCSharedPayloadsMapping.dat` (magic `0xABBA0003`): a payload de-duplication table. When a SNO's own payload section is empty, its pixel/blob bytes physically live under a different ("shared") SNO; this maps the requesting id → the id that actually holds the payload. Texture atlases (e.g. the per-class paragon node icons) rely on this. |
| struct [SnoEntry](./WiseOwl.Casc.Diablo4/SnoEntry.md) | One resolved `CoreTOC.dat` entry: a SNO record's group, numeric id, and name. |
| enum [SnoFolder](./WiseOwl.Casc.Diablo4/SnoFolder.md) | Which Diablo IV content partition a SNO is addressed in. A definition/record SNO lives in Meta; texture pixels live in Payload (with smaller mips in PayLow / PayMed). |
| enum [SnoGroup](./WiseOwl.Casc.Diablo4/SnoGroup.md) | Diablo IV SNO ("Sno", a typed game-object id namespace) group ids. Only the groups this module currently consumes are named; the full set has up to ~154 groups, all enumerable from [`CoreToc`](./WiseOwl.Casc.Diablo4/CoreToc.md). |
| struct [StateElements](./WiseOwl.Casc.Diablo4/StateElements.md) | One row of the §7.2 state contract: the back→front layer list for a (rarity, state) or a kind/overlay. Rows the schema enumerates but no scene widget binds (e.g. `overlay.selectionRing` — the selected-state red ring lives composited inside each per-rarity selected variant, not as a separate overlay) carry [`Layers`](./WiseOwl.Casc.Diablo4/StateElements/Layers.md) empty and [`Unresolved`](./WiseOwl.Casc.Diablo4/StateElements/Unresolved.md) = `true`. |
| class [StatTagDefinition](./WiseOwl.Casc.Diablo4/StatTagDefinition.md) | A decoded Diablo IV stat-threshold tag record (SNO group StatTag = 124). Surfaces the [`ThresholdFormulaText`](./WiseOwl.Casc.Diablo4/StatTagDefinition/ThresholdFormulaText.md) only — evaluation (substituting the `ParagonBoardEquipIndex` binding and reducing the expression to a numeric threshold) is the consumer's, mirroring the [`AttributeFormulaTable`](./WiseOwl.Casc.Diablo4/AttributeFormulaTable.md) boundary in `docs/casc-diablo4-format.md` Appendix C. |
| enum [StatUnit](./WiseOwl.Casc.Diablo4/StatUnit.md) | How a [`FlatValue`](./WiseOwl.Casc.Diablo4/ParagonNodeStat/FlatValue.md) should be rendered for display. Flat means "show as a raw number" (`+5 Strength`); Percent means "show with a percent sign" (`+7.5% Total Armor`); Multiplier is reserved for future stats that display as a multiplicative factor (`×1.5`) — no paragon node uses it today. |
| class [StringListCatalog](./WiseOwl.Casc.Diablo4/StringListCatalog.md) | A parsed per-locale Diablo IV StringList catalog (`base/StringList-Text-<locale>.dat`): every localized `label → text` string in the game for one locale, grouped by table (SNO). This is genuinely a generic D4-container concern, so it lives in the library; consumers map a table by name/SNO then look up labels. |
| class [StringListTable](./WiseOwl.Casc.Diablo4/StringListTable.md) | One Diablo IV StringList table (a single `.stl` / SNO group 42 definition): an ordered set of `label → localized text` pairs. A table is a domain bucket — e.g. `AttributeDescriptions`, `Bnet_Chat`, a skill/affix/item table. |
| struct [TexFrame](./WiseOwl.Casc.Diablo4/TexFrame.md) | One atlas sub-rect (`TexFrame`, 36 bytes) from a [`TextureDefinition`](./WiseOwl.Casc.Diablo4/TextureDefinition.md)'s `ptFrame` array — carries both the outer UV rect (the full sprite slot, including any authored padding) and an inner UV rect (the trimmed-content / 9-slice-middle rect) decoded from the trailing 16 bytes of the on-disk frame (CL-71, FR-C20 #32 codec-tail investigation). |
| enum [TextureCodec](./WiseOwl.Casc.Diablo4/TextureCodec.md) | The pixel codec a [`Format`](./WiseOwl.Casc.Diablo4/TextureDefinition/Format.md) selects. |
| static class [TextureDecoder](./WiseOwl.Casc.Diablo4/TextureDecoder.md) | Clean-room block-compression (S3TC/DXT) decoder for the codecs Diablo IV UI atlases use. Implements BC1 (DXT1) and BC3 (DXT5) — the paragon node/glyph atlases are BC3. BC2/BC7/etc. are out of scope here (the catalog reports the codec via [`Codec`](./WiseOwl.Casc.Diablo4/TextureDefinition/Codec.md); callers can pick another decoder if needed). No external dependency. |
| record [TextureDefinition](./WiseOwl.Casc.Diablo4/TextureDefinition.md) | A parsed Diablo IV `TextureDefinition`: pixel format, base dimensions, the per-mip `serTex` table, and the atlas `ptFrame` sub-rects. The pixel payload itself is fetched separately (it is addressable by SNO id); this metadata only lives in the combined bundle (see [`CombinedTextureMeta`](./WiseOwl.Casc.Diablo4/CombinedTextureMeta.md)). |
| record [TiledStyleBinding](./WiseOwl.Casc.Diablo4/TiledStyleBinding.md) | FR-C14 R9 — one binding of a UI widget's `snoTiledStyle` field to a [`TiledStyleDefinition`](./WiseOwl.Casc.Diablo4/TiledStyleDefinition.md). Surfaced on [`TiledStyleBindings`](./WiseOwl.Casc.Diablo4/ParagonBoardChrome/TiledStyleBindings.md) so consumers can reconstruct the engine's overlay composition for the paragon board. |
| record [TiledStyleDefinition](./WiseOwl.Casc.Diablo4/TiledStyleDefinition.md) | A decoded Diablo IV UI tile-style record (`.uis`, SNO group UiStyle = 103) — the engine's recipe for rendering a tiled UI overlay (vignette, inner-shadow, bag background, frame chrome, …) as a composition of texture pieces with a scale factor and padding. FR-C14 Phase 4 (CL-42). |
| struct [UiField](./WiseOwl.Casc.Diablo4/UiField.md) | One bound field of a [`UiWidget`](./WiseOwl.Casc.Diablo4/UiWidget.md): the field-name hash (`= Diablo4.FieldHash(name)`), the underlying `DT_*` type hash (`= Diablo4.TypeHash("DT_…")`), and the raw 32-bit bound value (interpretation — int / SNO handle / RGBA / enum / byte — is the consumer's, per Type of the field). |
| record [UiScene](./WiseOwl.Casc.Diablo4/UiScene.md) | A decoded Diablo IV UI-scene SNO (CoreTOC group `46`, format hash `0xE4825AB8`) — the reflection / data-binding widget graph behind screens such as the paragon board. This is the raw graph only: per widget, its name, class id, and the schema + bound field values exactly as serialized. No evaluation, imaging, layout, or policy is applied (that boundary is the consumer's — see `docs/casc-diablo4-format.md` Appendix C and §10). For the typed paragon projection use [`ReadParagonRenderLayout`](./WiseOwl.Casc.Diablo4/Diablo4Storage/ReadParagonRenderLayout.md) (§7.1 of the FR-C7 contract). |
| record [UiWidget](./WiseOwl.Casc.Diablo4/UiWidget.md) | One widget in a [`UiScene`](./WiseOwl.Casc.Diablo4/UiScene.md): its inline name, its class id (`= Diablo4.TypeHash(class name)`), its bound fields in serialized order, and any [`ExtraLayerValues`](./WiseOwl.Casc.Diablo4/UiWidget/ExtraLayerValues.md) bound via the 0x58-block shape (FR-C8 — the start/gate composite layers). |
| record [UiWidgetChild](./WiseOwl.Casc.Diablo4/UiWidgetChild.md) | An anonymous child sub-record of a [`UiWidget`](./WiseOwl.Casc.Diablo4/UiWidget.md) (FR-C16 R9 / FR-C18). The Diablo IV UI scene serializes a composite node template (e.g. `Template_Node_Magic`) as a parent widget whose disc / ornate / overlay layers are name-less nested records — each a class id + `0xFFFFFFFF` sentinel followed by its own schema + positionally-keyed value records, structurally identical to a named widget minus the inline name. Decoding them structurally keeps each layer's authored rect paired with its `hImageFrame` handle. |
| struct [WidgetRect](./WiseOwl.Casc.Diablo4/WidgetRect.md) | An authored `DT_INT` bindable rect, exactly as stored (UI reference units; no pixels). Pitch is derived, not stored. |

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
