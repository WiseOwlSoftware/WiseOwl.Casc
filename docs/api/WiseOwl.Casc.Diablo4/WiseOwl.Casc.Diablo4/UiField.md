# UiField structure

One bound field of a [`UiWidget`](./UiWidget.md): the field-name hash (`= Diablo4.FieldHash(name)`), the underlying `DT_*` type hash (`= Diablo4.TypeHash("DT_…")`), and the raw 32-bit bound value (interpretation — int / SNO handle / RGBA / enum / byte — is the consumer's, per Type of the field).

```csharp
public struct UiField : IEquatable<UiField>
```

| parameter | description |
| --- | --- |
| FieldHash | The 28-bit field-name hash. |
| TypeHash | The underlying `DT_*` type hash. |
| RawValue | The raw bound 32-bit value. |
| HasValue | `true` if a positional instance record supplied the value; `false` when the field is declared but unbound in this scene. |

## Public Members

| name | description |
| --- | --- |
| [UiField](UiField/UiField.md)(…) | One bound field of a [`UiWidget`](./UiWidget.md): the field-name hash (`= Diablo4.FieldHash(name)`), the underlying `DT_*` type hash (`= Diablo4.TypeHash("DT_…")`), and the raw 32-bit bound value (interpretation — int / SNO handle / RGBA / enum / byte — is the consumer's, per Type of the field). |
| [FieldHash](UiField/FieldHash.md) { get; set; } | The 28-bit field-name hash. |
| [HasValue](UiField/HasValue.md) { get; set; } | `true` if a positional instance record supplied the value; `false` when the field is declared but unbound in this scene. |
| [RawValue](UiField/RawValue.md) { get; set; } | The raw bound 32-bit value. |
| [TypeHash](UiField/TypeHash.md) { get; set; } | The underlying `DT_*` type hash. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [UiField.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/UiField.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
