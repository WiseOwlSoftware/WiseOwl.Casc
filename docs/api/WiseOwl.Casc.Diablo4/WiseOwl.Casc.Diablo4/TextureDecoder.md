# TextureDecoder class

Clean-room block-compression (S3TC/DXT) decoder for the codecs Diablo IV UI atlases use. Implements BC1 (DXT1) and BC3 (DXT5) — the paragon node/glyph atlases are BC3. BC2/BC7/etc. are out of scope here (the catalog reports the codec via [`Codec`](./TextureDefinition/Codec.md); callers can pick another decoder if needed). No external dependency.

```csharp
public static class TextureDecoder
```

## Public Members

| name | description |
| --- | --- |
| static [DecodeMip0](TextureDecoder/DecodeMip0.md)(…) | Decode mip 0 of a texture payload to a [`DecodedImage`](./DecodedImage.md). Diablo IV stores BC rows aligned up to 64 px; this decodes at the aligned width and crops back to the true [`Width`](./TextureDefinition/Width.md)× [`Height`](./TextureDefinition/Height.md). mip0 is taken from `SerTex[0]` when present (else payload offset 0 — the paragon atlas case). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [TextureDecoder.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/TextureDecoder.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
