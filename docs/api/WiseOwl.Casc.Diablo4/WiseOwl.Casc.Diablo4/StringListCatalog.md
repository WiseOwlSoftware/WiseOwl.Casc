# StringListCatalog class

A parsed per-locale Diablo IV StringList catalog (`base/StringList-Text-<locale>.dat`): every localized `label → text` string in the game for one locale, grouped by table (SNO). This is genuinely a generic D4-container concern, so it lives in the library; consumers map a table by name/SNO then look up labels.

```csharp
public sealed class StringListCatalog
```

## Public Members

| name | description |
| --- | --- |
| static [Parse](StringListCatalog/Parse.md)(…) | Parse a per-locale StringList bundle. *nameOf* maps a table's SNO id to its CoreTOC name (optional, for diagnostics). |
| [Locale](StringListCatalog/Locale.md) { get; } | The locale this catalog was parsed for (e.g. `enUS`). |
| [TableCount](StringListCatalog/TableCount.md) { get; } | Number of tables (StringList SNOs). |
| [Tables](StringListCatalog/Tables.md) { get; } | All tables, keyed by SNO id. |
| [Table](StringListCatalog/Table.md)(…) | Get a table by its SNO id. |
| [TryGet](StringListCatalog/TryGet.md)(…) | Resolve `label` within a specific table (SNO). (2 methods) |
| const [Magic](StringListCatalog/Magic.md) | The combined-meta magic (shared with the texture catalog). |

## Remarks

The per-locale file is the same `0x44CF00F5` combined-meta container as the texture catalog, but each entry's body is a StringListDefinition and the per-entry base differs (see `docs/casc-diablo4-format.md §6.3` — fully reverse-engineered and validated bundle-wide against the live build):

* `u32 magic (0x44CF00F5); u32 count; count × { i32 sno; u32 size }`
* per entry `i` (in index order), with `prevEnd` starting at `8 + count*8`: `B = alignUp8(prevEnd)` (the body base — no`+8`, and the SNO id comes from the index, not the body); `prevEnd = B + size[i]`.
* StringListDefinition body: `u32 infoLength @ B+20`; `entryCount = infoLength / 40`; entries at `B+32`, stride 40: `{ i64 pad; u32 keyOffset@+8; u32 keyLen@+12; i64 pad; u32 valOffset@+24; u32 valLen@+28; i64 pad }`. Strings are UTF-8 at `B + offset`, `len` bytes (trailing NUL stripped).

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [StringListCatalog.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/StringListCatalog.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
