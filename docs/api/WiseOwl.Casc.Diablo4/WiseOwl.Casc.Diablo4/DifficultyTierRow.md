# DifficultyTierRow class

One [`DifficultyTiersTable`](./DifficultyTiersTable.md) row — a monster level's scaling coefficients (FR-C34, CL-101). Carries the verified/inferred typed columns plus the full raw column vector for the unlabeled coefficients.

```csharp
public sealed class DifficultyTierRow
```

## Public Members

| name | description |
| --- | --- |
| [Columns](DifficultyTierRow/Columns.md) { get; } | Every column of the 128-byte row reinterpreted as Single (32 entries; index c = byte column `+4·c`). Caveats:`Columns[0]` is the level as an `int32` (use [`Level`](./DifficultyTierRow/Level.md)), and columns 3/4/5/6 (bytes `+12..+24`) are small `int` flags, not floats — reinterpret those as `int` via Single). The labeled columns (1/2/9/10) are the typed accessors above; the rest are unlabeled per-level reward/scaling coefficients, exposed raw so no data is hidden (comprehensive-data-exposure) without asserting names that can't be verified. |
| [Level](DifficultyTierRow/Level.md) { get; } | The 1-based monster level (col `+0`, an `int32`). Row i ↔ level i+1. |
| [MonsterDamageScalar](DifficultyTierRow/MonsterDamageScalar.md) { get; } | Per-level monster damage multiplier (col `+8`). Semantic inferred. |
| [MonsterHpScalar](DifficultyTierRow/MonsterHpScalar.md) { get; } | Per-level monster HP multiplier (col `+4`). Semantic inferred — see [`DifficultyTiersTable`](./DifficultyTiersTable.md) remarks. |
| [PerLevelGoldValue](DifficultyTierRow/PerLevelGoldValue.md) { get; } | Per-level gold value (col `+40`; candidate). |
| [PerLevelXpValue](DifficultyTierRow/PerLevelXpValue.md) { get; } | Per-level XP value (col `+36`) — the anchor column (L40 = 8.0, L70 = 11.0). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [DifficultyTierRow.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/DifficultyTierRow.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
