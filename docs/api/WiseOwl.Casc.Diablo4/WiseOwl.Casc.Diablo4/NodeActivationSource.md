# NodeActivationSource enumeration

FR-C16 R11 — where a [`NodeActivation`](./NodeActivation.md)'s condition was recovered from. The honesty marker for the `feedback_widget-name-not-role` discipline: a `SceneField` activation is read verbatim from the data; a `NameConvention` one is decoded from the engine's own state-suffixed identifier; an `EngineBehavior` one is CASC's documented inference where the name is suggestive but not literal.

```csharp
public enum NodeActivationSource
```

## Values

| name | value | description |
| --- | --- | --- |
| NameConvention | `0` | The widget/field name literally spells the state (e.g. `Node_Purchased` → Purchased, `Template_Node_Magic` → KindMagic). |
| SceneField | `1` | Read verbatim from a scene field (none exist for activation in scene 657304 — reserved for a future build that authors one). |
| EngineBehavior | `2` | CASC's documented engine-behavior inference where the name is suggestive but not literal (e.g. `NodeAvailableGlow` → Purchasable). Validate against the owner oracle before relying on it. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [NodeActivationSource.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/NodeActivationSource.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
