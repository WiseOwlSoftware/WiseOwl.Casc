# AssetLink structure

FR-C20 P5 — one authored relationship edge from an asset to another (e.g. a board to one of its nodes, a node to its passive power, a glyph to an affix it grants). The [`Target`](./AssetLink/Target.md) is a full [`AssetRef`](./AssetRef.md), so traversal chains: [`Related`](./Catalog/Related.md) the target again. Only authored FK edges are surfaced — runtime relationships (e.g. which glyph a player has socketed into a node) are not links.

```csharp
public struct AssetLink : IEquatable<AssetLink>
```

| parameter | description |
| --- | --- |
| Role | The edge label (e.g. `node`, `power`, `affix`, `class`). |
| Target | The related asset. |

## Public Members

| name | description |
| --- | --- |
| [AssetLink](AssetLink/AssetLink.md)(…) | FR-C20 P5 — one authored relationship edge from an asset to another (e.g. a board to one of its nodes, a node to its passive power, a glyph to an affix it grants). The [`Target`](./AssetLink/Target.md) is a full [`AssetRef`](./AssetRef.md), so traversal chains: [`Related`](./Catalog/Related.md) the target again. Only authored FK edges are surfaced — runtime relationships (e.g. which glyph a player has socketed into a node) are not links. |
| [Role](AssetLink/Role.md) { get; set; } | The edge label (e.g. `node`, `power`, `affix`, `class`). |
| [Target](AssetLink/Target.md) { get; set; } | The related asset. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [AssetLink.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/AssetLink.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
