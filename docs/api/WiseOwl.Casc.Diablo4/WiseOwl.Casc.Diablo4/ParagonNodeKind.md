# ParagonNodeKind enumeration

The display-axis classification of a paragon node — what the in-game tooltip headline shows (the visual archetype, distinct from [`ParagonRarity`](./ParagonRarity.md)). Derived from [`NodeType`](./ParagonNodeDefinition/NodeType.md) + [`IsGate`](./ParagonNodeDefinition/IsGate.md) + [`HasSocket`](./ParagonNodeDefinition/HasSocket.md) + [`Rarity`](./ParagonNodeDefinition/Rarity.md).

```csharp
public enum ParagonNodeKind
```

## Values

| name | value | description |
| --- | --- | --- |
| Normal | `0` | A plain board node (no socket, no gate, common rarity). |
| Magic | `1` | A magic-rarity stat node. |
| Rare | `2` | A rare-rarity stat node (carries the "bonus when threshold met" mechanic — see [`BonusStatTagSnoIds`](./ParagonNodeDefinition/BonusStatTagSnoIds.md)). |
| Legendary | `3` | A legendary-rarity node. |
| Start | `4` | The board's start node (`eNodeType == 5`) — the class emblem that anchors the board chain; carries no stat grants. |
| Socket | `5` | A glyph-socket node (`bHasSocket`) — grants its stat indirectly through the seated glyph and the surrounding magic nodes; carries no direct stat grants. |
| Gate | `6` | A board-attachment gate node (`bIsGate`) — the engine's user-facing name is "Board Attachment Gate". Structurally marks the connector slot a paragon board attaches through; the four-stat grant (`+5` Strength / Intelligence / Willpower / Dexterity, sampled on every Gate node) is regular stat content surfaced via [`Stats`](./ParagonNodeInfo/Stats.md). |

## Remarks

Precedence (a node can satisfy more than one of the underlying fields; the kind picks the most specific archetype): Start → Gate → Socket → then by rarity (Normal / Magic / Rare / Legendary).

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonNodeKind.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonNodeKind.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
