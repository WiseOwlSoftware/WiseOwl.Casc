# ParagonBoardChrome record

The paragon board chrome render model (§10.16). The [`MainBoardBackground`](./ParagonBoardChrome/MainBoardBackground.md) layer is the dark textured field drawn behind the node grid in scene 657304 (bound on `Template_Board_Background_Center`); [`BoardSelectChrome`](./ParagonBoardChrome/BoardSelectChrome.md) carries the board-select panel's preview frame and filigree band from scene 964599. All chrome layers carry no authored sub-rect — the scene leaves them engine-positioned at native pixel size. The animated rim "fire border" of the main board is **engine-internal**: its candidate atlas frames live in `2DUI_Paragon` (SNO 447106) but no scene widget in either scene binds them — CASC does not fabricate a typed sequence for engine-internal art (CL-28 / CL-30 discipline), so they are not surfaced in this model.

```csharp
public record ParagonBoardChrome
```

## Public Members

| name | description |
| --- | --- |
| [ParagonBoardChrome](ParagonBoardChrome/ParagonBoardChrome.md)(…) | The paragon board chrome render model (§10.16). The [`MainBoardBackground`](./ParagonBoardChrome/MainBoardBackground.md) layer is the dark textured field drawn behind the node grid in scene 657304 (bound on `Template_Board_Background_Center`); [`BoardSelectChrome`](./ParagonBoardChrome/BoardSelectChrome.md) carries the board-select panel's preview frame and filigree band from scene 964599. All chrome layers carry no authored sub-rect — the scene leaves them engine-positioned at native pixel size. The animated rim "fire border" of the main board is **engine-internal**: its candidate atlas frames live in `2DUI_Paragon` (SNO 447106) but no scene widget in either scene binds them — CASC does not fabricate a typed sequence for engine-internal art (CL-28 / CL-30 discipline), so they are not surfaced in this model. |
| [BoardSelectChrome](ParagonBoardChrome/BoardSelectChrome.md) { get; set; } |  |
| [MainBoardBackground](ParagonBoardChrome/MainBoardBackground.md) { get; set; } |  |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonBoardChrome.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonBoardChrome.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
