# ParagonBoardGrid record

FR-C17 — the engine's paragon-board grid-layout metric, in the authored design-canvas reference units. The consumer maps a board cell's grid coordinate to a canvas position with `canvasPos = (gridX·Pitch, gridY·Pitch)` (relative to the grid origin) and scales the whole canvas `CanvasWidth×CanvasHeight → render resolution`. This replaces the consumer's empirical pitch (the FR-C7-measured ~67.7px / arbitrary CellPx) with the engine's authored metric.  All values are read from the live UI scene (game data), not hard-coded: [`CanvasWidth`](./ParagonBoardGrid/CanvasWidth.md)/[`CanvasHeight`](./ParagonBoardGrid/CanvasHeight.md) from the root `ParagonBoard_main` widget, [`CellExtent`](./ParagonBoardGrid/CellExtent.md) from the `Template_Node_Common` node-cell widget. The board's logical grid (dimensions + which cell holds which node) is the per-board [`ParagonBoardDefinition`](./ParagonBoardDefinition.md) (`Width` + `Cells`); this record is the global canvas/cell metric shared across boards. Pitch = CellExtent (cells are laid out adjacent, no extra inter-cell gap beyond the per-cell art inset). Validated against the owner's in-game measurement: the empirical ~67.7px pitch = `CellExtent (100) × render-scale (≈0.677 at the consumer's board width)`, so the authored 100-unit cell reproduces the observed spacing exactly. If a future build introduces an explicit inter-cell gap it would surface as `Pitch ≠ CellExtent` (engine render-code RE — the `UIParagonBoardStyle` class is a style wrapper with no grid-layout fields, so any gap lives in the engine's grid-layout code, not the scene data).

```csharp
public record ParagonBoardGrid
```

## Public Members

| name | description |
| --- | --- |
| [ParagonBoardGrid](ParagonBoardGrid/ParagonBoardGrid.md)(…) | FR-C17 — the engine's paragon-board grid-layout metric, in the authored design-canvas reference units. The consumer maps a board cell's grid coordinate to a canvas position with `canvasPos = (gridX·Pitch, gridY·Pitch)` (relative to the grid origin) and scales the whole canvas `CanvasWidth×CanvasHeight → render resolution`. This replaces the consumer's empirical pitch (the FR-C7-measured ~67.7px / arbitrary CellPx) with the engine's authored metric.  All values are read from the live UI scene (game data), not hard-coded: [`CanvasWidth`](./ParagonBoardGrid/CanvasWidth.md)/[`CanvasHeight`](./ParagonBoardGrid/CanvasHeight.md) from the root `ParagonBoard_main` widget, [`CellExtent`](./ParagonBoardGrid/CellExtent.md) from the `Template_Node_Common` node-cell widget. The board's logical grid (dimensions + which cell holds which node) is the per-board [`ParagonBoardDefinition`](./ParagonBoardDefinition.md) (`Width` + `Cells`); this record is the global canvas/cell metric shared across boards. Pitch = CellExtent (cells are laid out adjacent, no extra inter-cell gap beyond the per-cell art inset). Validated against the owner's in-game measurement: the empirical ~67.7px pitch = `CellExtent (100) × render-scale (≈0.677 at the consumer's board width)`, so the authored 100-unit cell reproduces the observed spacing exactly. If a future build introduces an explicit inter-cell gap it would surface as `Pitch ≠ CellExtent` (engine render-code RE — the `UIParagonBoardStyle` class is a style wrapper with no grid-layout fields, so any gap lives in the engine's grid-layout code, not the scene data). |
| [CanvasHeight](ParagonBoardGrid/CanvasHeight.md) { get; set; } | Design-canvas height (`ParagonBoard_main.nHeight`; 1200). |
| [CanvasWidth](ParagonBoardGrid/CanvasWidth.md) { get; set; } | Design-canvas width in reference units (`ParagonBoard_main.nWidth`; 1920 on the current build). |
| [CellExtent](ParagonBoardGrid/CellExtent.md) { get; set; } | Node-cell extent in reference units (`Template_Node_Common.nWidth` = `nHeight`; 100). |
| [Pitch](ParagonBoardGrid/Pitch.md) { get; set; } | Cell-to-cell step in reference units. Equals [`CellExtent`](./ParagonBoardGrid/CellExtent.md) on the current build (adjacent cells). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonBoardGrid.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonBoardGrid.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
