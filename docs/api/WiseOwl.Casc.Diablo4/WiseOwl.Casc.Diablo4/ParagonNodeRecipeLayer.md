# ParagonNodeRecipeLayer record

FR-C16 — one layer of the [`ParagonNodeRecipe`](./ParagonNodeRecipe.md).

```csharp
public record ParagonNodeRecipeLayer
```

## Public Members

| name | description |
| --- | --- |
| [ParagonNodeRecipeLayer](ParagonNodeRecipeLayer/ParagonNodeRecipeLayer.md)(…) | FR-C16 — one layer of the [`ParagonNodeRecipe`](./ParagonNodeRecipe.md). |
| [Alpha](ParagonNodeRecipeLayer/Alpha.md) { get; set; } | The layer's `dwAlpha` opacity byte (`0xFF` when unspecified). |
| [CompositeHandles](ParagonNodeRecipeLayer/CompositeHandles.md) { get; set; } | FR-C16 R5 — the layer's additional texture handles bound via the multi-layer (0x58-block) path, beyond the single [`ImageHandle`](./ParagonNodeRecipeLayer/ImageHandle.md), in scene order. Resolvable handles only — the interleaved 0x58-block `int` params (rect insets, including small-negative overscan values such as `0xFFFFFFFD` = −3 on the larger Legendary disc) are excluded by the same icon-catalog validator used across FR-C9..C12 (CL-46 surfaced the negatives as bogus handles — fixed here). For the per-rarity sub-templates the rarity disc state pair is lifted out into [`SelectionDiscs`](./ParagonNodeRecipeLayer/SelectionDiscs.md); what remains here is the rarity's always-drawn interior-fill handle(s) (e.g. `Template_Node_Magic` → `0xFEC31E48`). For the non-rarity composite templates (`Template_Node_Starter` / `Quest` / `Socketable`) this is the full ordered layer stack. Empty for ordinary single-handle layers. |
| [ImageHandle](ParagonNodeRecipeLayer/ImageHandle.md) { get; set; } | The layer's `hImageFrame` texture handle. `0` ⇒ no static art: either a runtime-bound slot (the node's own icon/symbol, the equipped-glyph image) or a pure-predicate marker widget — surfaced (not dropped) so the consumer sees the full program. |
| [Rect](ParagonNodeRecipeLayer/Rect.md) { get; set; } | The layer's authored reference-unit rect (inset within the node cell; `default` ⇒ inherits the node template extent). |
| [SelectionDiscs](ParagonNodeRecipeLayer/SelectionDiscs.md) { get; set; } | FR-C16 R5 — non-`null` only for the rarity sub-templates (`Template_Node_Magic`/`Rare`/`Legendary`): the per-selection-state disc composite (unselected vs selected). CL-46 flattened both states into one `CompositeHandles` list, so a consumer drawing the list verbatim painted the selected-state ring on unselected nodes; this splits them so the interpreter draws [`Selected`](./NodeSelectionDiscs/Selected.md) only on the currently selected node and [`Unselected`](./NodeSelectionDiscs/Unselected.md) otherwise. See [`NodeSelectionDiscs`](./NodeSelectionDiscs.md). |
| [WidgetClassId](ParagonNodeRecipeLayer/WidgetClassId.md) { get; set; } | The widget's class style id (e.g. `UIWindowStyle` = drawable rect, `UIBlinkerStyle` = pulsing glow). Resolve via [`KnownTypeNames`](./Diablo4/KnownTypeNames.md). |
| [WidgetName](ParagonNodeRecipeLayer/WidgetName.md) { get; set; } | The engine's widget name, verbatim — the state discriminator the consumer maps to a runtime predicate (e.g. `Node_IconBase`, `Node_Purchased`, `Node_Purchasable`, `NodeAvailableGlow`, `Arrow_Top`/`Right`/`Bottom`/`Left`, `Connector_*`, `GlyphNodeGlow_*`, `Rarity_Display`, `Template_Node_Magic`/`Rare`/`Legendary`/…). |
| [ZOrder](ParagonNodeRecipeLayer/ZOrder.md) { get; set; } | The layer's draw order — its index in the scene serialization (lower draws first / underneath). The engine composes a node by drawing the predicate-satisfied layers in this order. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonNodeRecipeLayer.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonNodeRecipeLayer.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
