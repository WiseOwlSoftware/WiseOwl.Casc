# FormulaRange structure

One `arRanges` row of an [`AttributeFormula`](./AttributeFormula.md): a power-range start, its two range values, and the formula source text. Raw decoded fields — the library never evaluates the text.

```csharp
public struct FormulaRange : IEquatable<FormulaRange>
```

| parameter | description |
| --- | --- |
| ItemPowerRangeStart | `nItemPowerRangeStart` (`+0`). |
| RangeValue1 | First range value (`+4`, float). |
| RangeValue2 | Second range value (`+8`, float). |
| FormulaText | The `DT_STRING_FORMULA` source text (e.g. `"5"`, `"2 * ParagonPowerBudgetMultiplierNodeMagicOffensive()"`). |

## Public Members

| name | description |
| --- | --- |
| [FormulaRange](FormulaRange/FormulaRange.md)(…) | One `arRanges` row of an [`AttributeFormula`](./AttributeFormula.md): a power-range start, its two range values, and the formula source text. Raw decoded fields — the library never evaluates the text. |
| [FormulaText](FormulaRange/FormulaText.md) { get; set; } | The `DT_STRING_FORMULA` source text (e.g. `"5"`, `"2 * ParagonPowerBudgetMultiplierNodeMagicOffensive()"`). |
| [ItemPowerRangeStart](FormulaRange/ItemPowerRangeStart.md) { get; set; } | `nItemPowerRangeStart` (`+0`). |
| [RangeValue1](FormulaRange/RangeValue1.md) { get; set; } | First range value (`+4`, float). |
| [RangeValue2](FormulaRange/RangeValue2.md) { get; set; } | Second range value (`+8`, float). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [FormulaRange.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/FormulaRange.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
