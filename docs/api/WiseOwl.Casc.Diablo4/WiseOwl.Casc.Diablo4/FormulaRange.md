# FormulaRange structure

One `arRanges` row of an [`AttributeFormula`](./AttributeFormula.md): the item-power band this row applies to, its output clamps, and the formula source text. Raw decoded fields — the library never evaluates the text.

```csharp
public struct FormulaRange : IEquatable<FormulaRange>
```

| parameter | description |
| --- | --- |
| ItemPowerRangeStart | `nItemPowerRangeStart` (`+0`) — the row applies when the item's item power is `≥` this and below the next row's start (the last row covers all higher item powers). |
| RangeValue1 | CL-95 — the output lower clamp (`+4`), not the roll minimum. The evaluated value is pinned to `[RangeValue1, RangeValue2]` — across the table these are round, formula-independent bounds (e.g. `0`/`100` for a percent, `1`/`9999` for a core stat). The roll min/max come from the roll functions' arguments inside [`FormulaText`](./FormulaRange/FormulaText.md), not from these — see `casc-diablo4-format.md §8.1`. |
| RangeValue2 | CL-95 — the output upper clamp (`+8`); see [`RangeValue1`](./FormulaRange/RangeValue1.md). |
| FormulaText | The `DT_STRING_FORMULA` source text (e.g. `"5"`, `"FloatRandomRangeWithInterval(1,3,3.5)/100"`, `"2 * ParagonPowerBudgetMultiplierNodeMagicOffensive()"`). The value range is `[eval with each roll fn at its low arg, eval at its high arg]` clamped to `[RangeValue1, RangeValue2]`; the function contracts are in `casc-diablo4-format.md §8.1`. |

## Public Members

| name | description |
| --- | --- |
| [FormulaRange](FormulaRange/FormulaRange.md)(…) | One `arRanges` row of an [`AttributeFormula`](./AttributeFormula.md): the item-power band this row applies to, its output clamps, and the formula source text. Raw decoded fields — the library never evaluates the text. |
| [FormulaText](FormulaRange/FormulaText.md) { get; set; } | The `DT_STRING_FORMULA` source text (e.g. `"5"`, `"FloatRandomRangeWithInterval(1,3,3.5)/100"`, `"2 * ParagonPowerBudgetMultiplierNodeMagicOffensive()"`). The value range is `[eval with each roll fn at its low arg, eval at its high arg]` clamped to `[RangeValue1, RangeValue2]`; the function contracts are in `casc-diablo4-format.md §8.1`. |
| [ItemPowerRangeStart](FormulaRange/ItemPowerRangeStart.md) { get; set; } | `nItemPowerRangeStart` (`+0`) — the row applies when the item's item power is `≥` this and below the next row's start (the last row covers all higher item powers). |
| [RangeValue1](FormulaRange/RangeValue1.md) { get; set; } | CL-95 — the output lower clamp (`+4`), not the roll minimum. The evaluated value is pinned to `[RangeValue1, RangeValue2]` — across the table these are round, formula-independent bounds (e.g. `0`/`100` for a percent, `1`/`9999` for a core stat). The roll min/max come from the roll functions' arguments inside [`FormulaText`](./FormulaRange/FormulaText.md), not from these — see `casc-diablo4-format.md §8.1`. |
| [RangeValue2](FormulaRange/RangeValue2.md) { get; set; } | CL-95 — the output upper clamp (`+8`); see [`RangeValue1`](./FormulaRange/RangeValue1.md). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [FormulaRange.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/FormulaRange.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
