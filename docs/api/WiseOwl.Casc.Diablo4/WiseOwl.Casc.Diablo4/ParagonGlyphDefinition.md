# ParagonGlyphDefinition class

A decoded Diablo IV `ParagonGlyphDefinition` (`.gph`, SNO group 111). Raw fields only.

```csharp
public sealed class ParagonGlyphDefinition
```

## Public Members

| name | description |
| --- | --- |
| static [Parse](ParagonGlyphDefinition/Parse.md)(…) | Decode a ParagonGlyph from its raw SNO blob. |
| [AffixSnoIds](ParagonGlyphDefinition/AffixSnoIds.md) { get; } | The glyph's ParagonGlyphAffix SNO ids (0..3, in slot order). |
| [BaseRadius](ParagonGlyphDefinition/BaseRadius.md) { get; } | FR-C24 (CL-83) — the glyph's base socket-effect radius at Level 1. Engine constant on build `3.0.2.71886` — empirically `3` across all 21 Warlock glyphs in the Optimizer's oracle table; the `.gph` record itself has no `nStartingSize` field (payload ends at the affix-array descriptor; no per-glyph variance authored). Surfaced as a read-only constant so a future season's per-glyph variation triggers an Appendix D re-verify on this property rather than a silent drift in consumer code that hard-coded `3`. |
| [LocalizedTitle](ParagonGlyphDefinition/LocalizedTitle.md) { get; } | FR-C24 (CL-79) — the glyph's localized display title (e.g. `"Attrition"`, `"Guzzler"`, `"Abyssal"`) resolved via the §6.7 sibling-StringList convention (`Item_ParagonGlyph_<GlyphSnoName>`, label `Name`) with the universal `"Glyph: "` prefix stripped. Empty when the sibling table is missing or the glyph was decoded via the byte-only [`Parse`](./ParagonGlyphDefinition/Parse.md) (no [`CoreToc`](./CoreToc.md) to resolve the sibling name). Populated by [`ReadParagonGlyph`](./Diablo4Storage/ReadParagonGlyph.md). |
| [MaxLevel](ParagonGlyphDefinition/MaxLevel.md) { get; } | FR-C24 (CL-83) — the maximum player level a glyph can reach (the level cap that gates [`RadiusUpgradeLevels`](./ParagonGlyphDefinition/RadiusUpgradeLevels.md) and the per-level affix scaling). Engine constant on build `3.0.2.71886` — empirically `150`; the `ParagonGlyphExperienceTable` (sno `810212`) ships 201 XP-curve entries but the in-game cap is held at 150. Same re-verify trigger as [`BaseRadius`](./ParagonGlyphDefinition/BaseRadius.md) — if a future season raises the cap, this property's value needs updating + Appendix D re-verify. |
| [RadiusUpgradeLevels](ParagonGlyphDefinition/RadiusUpgradeLevels.md) { get; } | FR-C24 (CL-83) — the player levels at which the glyph's socket-effect radius grows by one. Engine constant on build `3.0.2.71886` — empirically `[25, 50]` across all 21 Warlock glyphs (radius is [`BaseRadius`](./ParagonGlyphDefinition/BaseRadius.md) at L1..L24, [`BaseRadius`](./ParagonGlyphDefinition/BaseRadius.md)+1 at L25..L49, [`BaseRadius`](./ParagonGlyphDefinition/BaseRadius.md)+2 at L50+). Same rationale + re-verify trigger as [`BaseRadius`](./ParagonGlyphDefinition/BaseRadius.md) — the `.gph` record carries no `arSizeUpgradeLevels` field. |
| [Rarity](ParagonGlyphDefinition/Rarity.md) { get; } | FR-C24 (CL-79) — the glyph's nominal [`ParagonRarity`](./ParagonRarity.md). Every glyph in the live build (3.0.2.71886) is authored as Rare (the SNO name follows `Rare_<NN>_<Stat>_<Slot>`); the field is exposed forward-looking should the engine add Magic / Legendary glyphs in a future build. The value comes from the SNO name's leading-token convention — the durable opaque-id principle (Appendix C) decodes naming-convention fields library-side and surfaces them typed. |
| [SnoId](ParagonGlyphDefinition/SnoId.md) { get; } | The glyph's own SNO id. |
| [UsableByClassSnoIds](ParagonGlyphDefinition/UsableByClassSnoIds.md) { get; } | The set of classes that may socket this glyph, as PlayerClass SNO ids — the shared class key (== [`SnoId`](./CharacterClass/SnoId.md) / [`ClassSnoId`](./ParagonBoardDefinition/ClassSnoId.md); FR-D3). Empty if decoded via the byte-only [`Parse`](./ParagonGlyphDefinition/Parse.md) (no [`CoreToc`](./CoreToc.md) to resolve the class ordering), or for a malformed/placeholder glyph record (honest empty sentinel — never a silently-wrong class). |

## Remarks

Byte layout per the canonical reference (`docs/casc-diablo4-format.md §7.3`, migrated/verified from upstream `d4-binary-formats.md §5` — the corrected glyph layout): payload base `0x10`; `snoId@0`; the glyph's affixes are a fixed set of up to three `DT_SNO` ids at payload `+104 / +108 / +112` (the 3 glyph power components — scaling mod / threshold / legendary). `0` and `0xFFFFFFFF` slots are treated as "no affix" and omitted.

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonGlyphDefinition.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonGlyphDefinition.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
