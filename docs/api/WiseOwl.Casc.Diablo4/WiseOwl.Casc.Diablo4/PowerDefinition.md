# PowerDefinition class

A decoded Diablo IV `PowerDefinition` (`.pow`, SNO group Power = 29) — a skill/passive/legendary power. Identity + localized text + the structured Script Formula slot table (FR-C13 Phase 1); the engine-internal AST evaluator that resolves non-trivial SF_N expressions to runtime values is the consumer's stat-effect model per the library boundary (Appendix C). FR-C13 Phase 2 (forthcoming) lifts the trivial-numeric resolution into the library; until then, expression-typed slots ([`IsExpression`](./PowerScriptFormula/IsExpression.md)) carry their raw text form (e.g. `"SF_1 / 3"`) for consumer-side evaluation.

```csharp
public sealed class PowerDefinition
```

## Public Members

| name | description |
| --- | --- |
| static [Parse](PowerDefinition/Parse.md)(…) | Decode a Power from its raw SNO blob. Identity + [`ScriptFormulas`](./PowerDefinition/ScriptFormulas.md) + [`ResolvedFormulas`](./PowerDefinition/ResolvedFormulas.md); the localized fields (and the [`FunctionRefs`](./PowerDefinition/FunctionRefs.md) scan, which reads them) need [`CoreToc`](./CoreToc.md); use [`ReadPower`](./Diablo4Storage/ReadPower.md). |
| [CompiledFormulas](PowerDefinition/CompiledFormulas.md) { get; } | FR-C13 Phase 3 — the engine's compiled-form `{SF_N → value}` map, derived from the binary `DT_STRING_FORMULA` records' IEEE-754 floats (for literal slots) and from binary AST opcode operands (for expression slots). The values are the engine-truth: the IEEE-754 singles the engine literally executes, promoted to Double.  Distinct from [`ResolvedFormulas`](./PowerDefinition/ResolvedFormulas.md) only when the engine has compiled a slot's text form (e.g. `"0.02"` → `0.02f`) inconsistently — in that case the binary IEEE-754 single is canonical and this map reflects it, while [`ResolvedFormulas`](./PowerDefinition/ResolvedFormulas.md) reflects the text-parsed value. In normal operation the two maps agree to float precision for all 9 Warlock legendary anchors (the FR-C13 R5 regression gate).  For expression slots (Demonic Spicules's `SF_2 = "SF_1 / 3"` is the canonical anchor): the operator is parsed from the slot's text, but numeric operands are substituted from the binary AST opcode region's embedded IEEE-754 singles in encounter order. So Demonic Spicules's `SF_2` evaluates as `SF_1 / binary_literal[0]` = `60 / 3.0f` = `20`; the `3.0f` comes from the compiled record's binary bytes, not from re-parsing the text `"3"`.  AST opcode encoding for non-literal sub-trees (e.g. function calls inside expression records) is not yet fully decoded (devlog 0035 §3). For powers whose compiled AST encoding is outside the current decoder's scope, the slot is left at NaN — the consumer compares non-NaN entries for cross-validation. |
| [Description](PowerDefinition/Description.md) { get; } | Localized power description (sibling label `desc`; carries D4 markup/substitution tokens), or Empty. |
| [FunctionRefs](PowerDefinition/FunctionRefs.md) { get; } | FR-C13 Phase 2 — engine-function references the power's formulas + localized [`Description`](./PowerDefinition/Description.md) rely on. The library surfaces each call (name + resolved-arg values) structurally; the consumer registers a per-name resolver to substitute the engine-runtime value (player-state accessors like `PlayerHealthMax()` are outside CASC's domain). Empty for the typical legendary passive (no function calls in the format string); non-empty for powers like Barbarian `Warbringer` whose format uses `[SF_1 * PlayerHealthMax()]`. |
| [Modifiers](PowerDefinition/Modifiers.md) { get; } | LIB-5 (CL-104) — the skill's selectable modifiers (the skill-tree enhancement / upgrade nodes: each active skill offers several, chosen in mutually-exclusive groups). Each carries its localized display [`Name`](./PowerModifier/Name.md) and the full effect [`Description`](./PowerModifier/Description.md) (with D4 markup/substitution tokens, like [`Description`](./PowerDefinition/Description.md)). Decoded from the power's sibling StringList (`Power_<snoName>`) `Mod<N>_Name` / `Mod<N>_Description` labels (the generalized §6.7 sibling convention). Ordered by the modifier index [`Index`](./PowerModifier/Index.md) (which is sparse — the engine skips unused slots). Empty for powers with no modifiers (passives, non-skill powers). The modifier group assignment (which choices are mutually exclusive) is not yet decoded — a follow-up; the names + effect text are complete. |
| [Name](PowerDefinition/Name.md) { get; } | Localized power name (sibling label `name`), or Empty if unresolved. See the type remarks. |
| [ResolvedFormulas](PowerDefinition/ResolvedFormulas.md) { get; } | FR-C13 Phase 2 — the resolved per-power `{SF_N → value}` map. Each key is `"SF_N"` (positionally matching the engine's `[SF_n...]` placeholders in [`Description`](./PowerDefinition/Description.md)); each value is the slot's resolved double — the IEEE-754 single-precision literal for trivial- numeric slots ([`LiteralValue`](./PowerScriptFormula/LiteralValue.md) promoted to Double) OR the evaluator's result for expression-text slots (e.g. Demonic Spicules's `"SF_1 / 3"` slot resolves to `60 / 3 = 20`).  Expression slots can reference other slots' values by SF_N; resolution iterates until a fixed point is reached or an unresolvable reference forces NaN for that slot. Function calls inside slot expressions (rare on legendary node powers; common on Barbarian / Necromancer active skills) surface as [`FunctionRefs`](./PowerDefinition/FunctionRefs.md) alongside the resolved value (the call returns NaN until the consumer registers a resolver).  Empty when the power has no [`ScriptFormulas`](./PowerDefinition/ScriptFormulas.md). |
| [ScriptFormulas](PowerDefinition/ScriptFormulas.md) { get; } | FR-C13 Phase 1 — the positional Script Formula slot table decoded from the Power record's tail data. Each entry is one `DT_STRING_FORMULA`'s compiled form: a text form (numeric like `"0.02"` or an arithmetic expression like `"SF_1 / 3"`) and the IEEE-754 literal value (the engine's pre-computed scalar when the text is numeric; NaN when the text is an expression). Indexed positionally: [`Index`](./PowerScriptFormula/Index.md) matches the `[SF_n...]` placeholders in [`Description`](./PowerDefinition/Description.md) (the engine's format-string SF_N indices).  Empty when the power has no script formulas (most active skills and many structural / informational powers fall here). Non-empty for the 72 legendary node powers + others using the `DT_STRING_FORMULA` mechanism. The trailing footer records (`"10"` / 10.0 followed by `"0"` / 0.0 on every anchored legendary) are stripped from this list — they are a fixed value-descriptor footer, not SF_N values and not the rank cap (FR-C38 corrected the CL-100 misread; the per-aspect max rank is [`MaxRank`](./AffixDefinition/MaxRank.md), the int32 at affix payload `+0x94`). |
| [SnoId](PowerDefinition/SnoId.md) { get; } | The power's own SNO id (== the CoreTOC id). |

## Remarks

[`SnoId`](./PowerDefinition/SnoId.md) is the binary field (payload `0`). The localized [`Name`](./PowerDefinition/Name.md) / [`Description`](./PowerDefinition/Description.md) are resolved from the power's sibling StringList table via the generalized sibling-string convention (`docs/casc-diablo4-format.md §11.2`, Appendix A CL-22 / CL-20): group-42 SNO `"Power_" + snoName`, labels `name` / `desc`. Empty (honest sentinel) when decoded byte-only (no [`CoreToc`](./CoreToc.md)) or when the power has no sibling table; the consumer owns any fallback. Raw decoded text — D4 markup kept intact.

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [PowerDefinition.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/PowerDefinition.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
