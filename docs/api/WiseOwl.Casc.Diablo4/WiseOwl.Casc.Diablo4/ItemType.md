# ItemType class

A decoded Diablo IV item base type — an entry in the item-type dictionary (SNO group ItemType = 98, the game's `eItemType` enum: `Sword`, `Amulet`, `Helm`, `Charm`, `HealthPotion`, …). Every [`ItemDefinition`](./ItemDefinition.md) references its base type ([`ItemTypeSnoId`](./ItemDefinition/ItemTypeSnoId.md)); this record is what classifies an item as a weapon / armor / jewelry / charm (LIB-1).

```csharp
public sealed class ItemType
```

## Public Members

| name | description |
| --- | --- |
| static [Parse](ItemType/Parse.md)(…) | Decode an item base type from its raw SNO blob and CoreTOC name. Malformed/short records classify as Other (non-equippable). |
| [Class](ItemType/Class.md) { get; } | The broad equipment category (weapon / armor / jewelry / charm / other), derived structurally from the record. |
| [EItemType](ItemType/EItemType.md) { get; } | The engine `eItemType`ordinal — the value that appears in an affix's [`AllowedItemTypes`](./AffixDefinition/AllowedItemTypes.md) pool (#51, CL-108). Decoded as the first entry of the record's item-type array (the `DT_VARIABLEARRAY` at payload `+0x28`). This is the missing link that names an affix pool: e.g. `Helm`=16, `ChestArmor`=17, `Gloves`=28, `Boots`=29, `Legs`=30, `Amulet`=26, `Ring`=19, `Axe`=1, `Bow`=10, `Charm`=71 (build 3.1.1.72836). 1H/2H and class variants share one ordinal (`Axe` and `Axe2H` are both `1`) — the ordinal is the base type, so a name lookup is one-ordinal-to-many-names. −1 when the record carries no item-type array (non-gear types) or it is malformed. A small number of ordinals that appear in affix pools (e.g. `9`, `23`) have no g98 [`ItemType`](./ItemType.md) record and so cannot be named from the data — they are engine-aggregate/legacy values; use [`GetItemTypeName`](./Diablo4Storage/GetItemTypeName.md) which returns `null` for those. |
| [IsEquippable](ItemType/IsEquippable.md) { get; } | Whether this is equippable gear (the kind word is `32`/`48`) as opposed to a consumable, currency, quest item, and so on. |
| [Name](ItemType/Name.md) { get; } | The base type's CoreTOC name (e.g. `"Sword"`, `"Amulet"`, `"Charm"`) — a durable, opaque stable id. |
| [SnoId](ItemType/SnoId.md) { get; } | The base type's own SNO id (== the CoreTOC id; the value an item's [`ItemTypeSnoId`](./ItemDefinition/ItemTypeSnoId.md) points at). |
| [WeaponFamily](ItemType/WeaponFamily.md) { get; } | The raw weapon-family enum (payload `+0x30`): a coarse grouping shared across related weapons (e.g. Axe/Sword/Mace all share one value), or `-1` when this is not a weapon-slot item. |

## Remarks

Byte layout (clean-room, `docs/casc-diablo4-format.md §13`): payload base `0x10`; `snoId` at payload `0`. The classification fields (all payload-relative): a kind word at `+0x08` (`32`/`48` ⇒ equippable gear; smaller values ⇒ non-gear), a sub-kind at `+0x0C` (`5` ⇒ Charm), a weapon-family enum at `+0x30` (`≥ 0` ⇒ a weapon-slot item, `-1` ⇒ not a weapon), an armor-value scalar at `+0x3C` (`0` ⇒ jewelry, `> 0` ⇒ body armor), and a slot word at `+0x44` (`> 0` for armor/jewelry). Verified across the full g98 set on build 3.1.1.72836. The type [`Name`](./ItemType/Name.md) is the CoreTOC name — a durable, opaque stable id (Appendix C).

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ItemType.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ItemType.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
