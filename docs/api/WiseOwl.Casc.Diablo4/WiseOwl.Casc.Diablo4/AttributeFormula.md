# AttributeFormula record

One named GameBalance attribute formula entry (`AttributeFormulaEntry`, stride 280).

```csharp
public record AttributeFormula
```

## Public Members

| name | description |
| --- | --- |
| [AttributeFormula](AttributeFormula/AttributeFormula.md)(…) | One named GameBalance attribute formula entry (`AttributeFormulaEntry`, stride 280). |
| [Name](AttributeFormula/Name.md) { get; set; } | `szName` (inline `DT_CHARARRAY[256]`), e.g. `ParagonNodeCoreStat_Normal`. |
| [NameGbid](AttributeFormula/NameGbid.md) { get; set; } | `GbidHash(Name)` — the entry's identity (the in-record `gbid` field is `0xFFFFFFFF`; identity is the name hash, matching a node's `FormulaGbid`). |
| [PrimaryText](AttributeFormula/PrimaryText.md) { get; } | The primary formula text (`arRanges[0].FormulaText`) — the value the consumer evaluates. Empty if the entry has no ranges. |
| [Ranges](AttributeFormula/Ranges.md) { get; set; } | The `arRanges` rows (≥1; paragon stat nodes have exactly one). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [AttributeFormula.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/AttributeFormula.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
