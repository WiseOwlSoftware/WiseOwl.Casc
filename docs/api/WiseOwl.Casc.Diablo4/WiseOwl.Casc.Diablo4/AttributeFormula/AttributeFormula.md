# AttributeFormula constructor

One named GameBalance attribute formula entry (`AttributeFormulaEntry`, stride 280).

```csharp
public AttributeFormula(string Name, uint NameGbid, IReadOnlyList<FormulaRange> Ranges)
```

| parameter | description |
| --- | --- |
| Name | `szName` (inline `DT_CHARARRAY[256]`), e.g. `ParagonNodeCoreStat_Normal`. |
| NameGbid | `GbidHash(Name)` — the entry's identity (the in-record `gbid` field is `0xFFFFFFFF`; identity is the name hash, matching a node's `FormulaGbid`). |
| Ranges | The `arRanges` rows (≥1; paragon stat nodes have exactly one). |

## See Also

* struct [FormulaRange](../FormulaRange.md)
* record [AttributeFormula](../AttributeFormula.md)
* namespace [WiseOwl.Casc.Diablo4](../../WiseOwl.Casc.Diablo4.md)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
