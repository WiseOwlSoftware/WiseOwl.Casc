# UiWidgetChild constructor

An anonymous child sub-record of a [`UiWidget`](../UiWidget.md) (FR-C16 R9 / FR-C18). The Diablo IV UI scene serializes a composite node template (e.g. `Template_Node_Magic`) as a parent widget whose disc / ornate / overlay layers are name-less nested records — each a class id + `0xFFFFFFFF` sentinel followed by its own schema + positionally-keyed value records, structurally identical to a named widget minus the inline name. Decoding them structurally keeps each layer's authored rect paired with its `hImageFrame` handle.

```csharp
public UiWidgetChild(uint ClassId, IReadOnlyList<UiField> Fields)
```

| parameter | description |
| --- | --- |
| ClassId | The child's class id (`= Diablo4.TypeHash(class)`; typically `UIWindowStyle`). |
| Fields | The child's bound fields, in serialized order (same shape as [`Fields`](../UiWidget/Fields.md)) — its `hImageFrame` handle, `nLeft/nTop/nRight/nBottom/nWidth/nHeight` rect insets, `bActive`, etc. |

## See Also

* struct [UiField](../UiField.md)
* record [UiWidgetChild](../UiWidgetChild.md)
* namespace [WiseOwl.Casc.Diablo4](../../WiseOwl.Casc.Diablo4.md)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
