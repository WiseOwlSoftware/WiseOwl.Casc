# NodeSlot enumeration

FR-C16 R11 — the composition slot a recipe layer competes for. Layers with the same slot are mutually-exclusive variants; the consumer draws the one whose [`Activation`](./ParagonNodeRecipeLayer/Activation.md) evaluates true for the node.

```csharp
public enum NodeSlot
```

## Values

| name | value | description |
| --- | --- | --- |
| None | `0` | No exclusivity — the layer composes independently. |
| BaseDisc | `1` | The node's base disc (grey / per-rarity / per-type variants all compete here — exactly one is drawn per node). |
| Symbol | `2` | The node's emblem/symbol slot (`Node_Icon` → the node's `HIconMask`). |
| Directional | `3` | A directional pointer/connector toward a neighbour. |
| Glow | `4` | A state glow overlay (available/revealed/purchased/equip). |
| Socket | `5` | The glyph-socket composite (glyph image + usage beads). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [NodeSlot.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/NodeSlot.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
