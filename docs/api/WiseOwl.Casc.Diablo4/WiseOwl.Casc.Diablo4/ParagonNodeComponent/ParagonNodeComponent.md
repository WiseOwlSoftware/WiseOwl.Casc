# ParagonNodeComponent constructor

FR-C16 R14 — one atomic drawable component of a [`ParagonNodeRecipe`](../ParagonNodeRecipe.md): a single atlas frame drawn at its rect/alpha when its [`Activation`](./Activation.md) holds. The flattening of the former layer/disc/composite nesting — every per-rarity disc, per-state ornate, interior fill, glow, arrow, and symbol slot is its own component with the exact combined condition.

```csharp
public ParagonNodeComponent(int ZOrder, string Source, uint ImageHandle, WidgetRect Rect, 
    byte Alpha, NodeActivation Activation, bool DefaultActive, RgbaTint? Tint)
```

| parameter | description |
| --- | --- |
| ZOrder | The component's true paint order — its index in [`Components`](../ParagonNodeRecipe/Components.md) (lower draws first / underneath). The per-rarity/-type disc is remapped to the base-disc position (below the symbol), NOT the template widget's appended scene position. |
| Source | The originating widget name (and child index for a template sub-record, e.g. `Template_Node_Magic[1]`) — verbatim, for traceability; never a normalized role. |
| ImageHandle | The component's atlas frame handle. `0` ⇒ a runtime-filled slot (e.g. `Node_Icon` → the node's `HIconMask`) — drawn, not dropped, so the program is complete. |
| Rect | The component's authored reference-unit rect inset (`default` ⇒ inherits the cell; negative ⇒ overscan). |
| Alpha | The component's `dwAlpha` opacity byte. |
| Activation | The exact engine-sourced condition gating this component (rarity/type ∧ selection-state combined). Evaluate against the consumer-computed fact set; see [`NodeActivation`](../NodeActivation.md). |
| DefaultActive | The layer's authored `bActive` — its default (resting) visibility. `true` (or unbound) ⇒ shown in the default/unselected state; `false` ⇒ default-off, shown only when the engine toggles it for a runtime state (selected, purchased, located, …). This is the field that disambiguates a swap pair: the `bActive=1` disc is the unselected variant, the `bActive=0` disc the selected one. |
| Tint | The layer's authored `rgbaTint` (multiply colour), or `null` for no tint. E.g. the glyph-socket base disc is drawn through a grey `0xFF8A8A8A` tint. |

## See Also

* struct [WidgetRect](../WidgetRect.md)
* record [NodeActivation](../NodeActivation.md)
* struct [RgbaTint](../RgbaTint.md)
* record [ParagonNodeComponent](../ParagonNodeComponent.md)
* namespace [WiseOwl.Casc.Diablo4](../../WiseOwl.Casc.Diablo4.md)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
