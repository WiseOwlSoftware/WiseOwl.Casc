# ParagonGlyphAffixOperation enumeration

The kind of bonus mechanism a [`ParagonGlyphAffixDefinition`](./ParagonGlyphAffixDefinition.md) applies — the typed view of the raw `eBonusOperation` int ([`Operation`](./ParagonGlyphAffixDefinition/Operation.md)) at payload `+0x30`. Values match the live `3.0.2.71886` build.

```csharp
public enum ParagonGlyphAffixOperation
```

## Values

| name | value | description |
| --- | --- | --- |
| Unknown | `0` | An operation value not observed on the live build. Reserved for forward-compat (a future season ships a new op type). |
| Attribute | `1` | Op 1 — `Attribute`: flat attribute grants. The affix adds [`Base`](./ParagonGlyphAffixDefinition/Base.md) + `(Level-1)` × [`PerLevel`](./ParagonGlyphAffixDefinition/PerLevel.md) to each [`AffectedAttributes`](./ParagonGlyphAffixDefinition/AffectedAttributes.md) entry. Used by node-bonus affixes (e.g. `Nodes_BonusToMinion`). The AffectedAttributes VLA descriptor for Op-1 lives at payload `+16/+20`. |
| NodeAmplification | `2` | Op 2 — `NodeAmplification`: percent-scaling affix against a tagged scope (Abyss / Archfiend / Bleeding / etc.). Both `_Main` ("per 5 &lt;Attribute&gt; → +X% &lt;Tag&gt; Skill") and `_Side` ("+X%[x] vs Tag") affixes carry this op. The AffectedAttributes VLA descriptor for Op-2 lives at payload `+64/+68`. |
| AttributeConversion | `4` | Op 4 — `AttributeConversion`: legendary `Mult*_Legendary` additive percentage that unlocks at glyph Level 50 (engine-side gate). Base / PerLevel are stored as fractions (×100 → percent). The AffectedAttributes VLA descriptor for Op-4 lives at payload `+104/+108`. |
| Power | `5` | Op 5 — `Power`: the affix references a [`LinkedPowerSnoId`](./ParagonGlyphAffixDefinition/LinkedPowerSnoId.md) (group 29 PowerDefinition) that defines the threshold chain / skill-cast behavior; [`Base`](./ParagonGlyphAffixDefinition/Base.md) and [`PerLevel`](./ParagonGlyphAffixDefinition/PerLevel.md) are zero (the magnitude lives in the linked power, not on the affix). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonGlyphAffixOperation.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonGlyphAffixOperation.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
