# NodeActivation record

FR-C16 R11 — the typed activation condition of a recipe layer: the engine-sourced predicate over [`NodeFact`](./NodeFact.md) that gates whether the layer draws. The consumer supplies the set of currently-true facts and calls [`Evaluate`](./NodeActivation/Evaluate.md); it authors no predicate of its own.

```csharp
public record NodeActivation
```

## Public Members

| name | description |
| --- | --- |
| [NodeActivation](NodeActivation/NodeActivation.md)(…) | FR-C16 R11 — the typed activation condition of a recipe layer: the engine-sourced predicate over [`NodeFact`](./NodeFact.md) that gates whether the layer draws. The consumer supplies the set of currently-true facts and calls [`Evaluate`](./NodeActivation/Evaluate.md); it authors no predicate of its own. |
| static [Always](NodeActivation/Always.md) { get; } | An always-on activation (no gating fact). |
| static [Of](NodeActivation/Of.md)(…) | Build a single-fact activation. |
| [AllOf](NodeActivation/AllOf.md) { get; set; } | The facts that must all hold for the layer to draw (logical AND). Empty ⇒ Always. A list containing Never never activates. |
| [Source](NodeActivation/Source.md) { get; set; } | Where the condition was recovered from — the honesty marker (see [`NodeActivationSource`](./NodeActivationSource.md)). |
| [Evaluate](NodeActivation/Evaluate.md)(…) | Evaluate the activation against the consumer-computed set of currently-true facts: the layer draws iff every fact in [`AllOf`](./NodeActivation/AllOf.md) is present (and none is Never). |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [NodeActivation.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/NodeActivation.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
