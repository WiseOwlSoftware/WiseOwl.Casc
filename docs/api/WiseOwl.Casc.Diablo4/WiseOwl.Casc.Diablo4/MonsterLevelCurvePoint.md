# MonsterLevelCurvePoint structure

One row of a [`MonsterLevelCurve`](./MonsterLevelCurve.md) — a 12-byte record (two `int32` + one `float32`). [`Level`](./MonsterLevelCurvePoint/Level.md)/[`LevelHigh`](./MonsterLevelCurvePoint/LevelHigh.md) are the (equal, in live data) monster level and [`ScaledValue`](./MonsterLevelCurvePoint/ScaledValue.md) the scaled effective value; the precise remap semantic is a structural inference (see [`MonsterLevelCurvesTable`](./MonsterLevelCurvesTable.md) remarks) — the raw fields are all exposed.

```csharp
public struct MonsterLevelCurvePoint : IEquatable<MonsterLevelCurvePoint>
```

| parameter | description |
| --- | --- |
| Level | First `int32` — the monster/area level (row +0). |
| LevelHigh | Second `int32` — equal to [`Level`](./MonsterLevelCurvePoint/Level.md) in the live data; the upper bound of the level band if it is one (row +4). |
| ScaledValue | The `float32` scaled value (row +8) — climbs to `100` across the tier's level span. |

## Public Members

| name | description |
| --- | --- |
| [MonsterLevelCurvePoint](MonsterLevelCurvePoint/MonsterLevelCurvePoint.md)(…) | One row of a [`MonsterLevelCurve`](./MonsterLevelCurve.md) — a 12-byte record (two `int32` + one `float32`). [`Level`](./MonsterLevelCurvePoint/Level.md)/[`LevelHigh`](./MonsterLevelCurvePoint/LevelHigh.md) are the (equal, in live data) monster level and [`ScaledValue`](./MonsterLevelCurvePoint/ScaledValue.md) the scaled effective value; the precise remap semantic is a structural inference (see [`MonsterLevelCurvesTable`](./MonsterLevelCurvesTable.md) remarks) — the raw fields are all exposed. |
| [Level](MonsterLevelCurvePoint/Level.md) { get; set; } | First `int32` — the monster/area level (row +0). |
| [LevelHigh](MonsterLevelCurvePoint/LevelHigh.md) { get; set; } | Second `int32` — equal to [`Level`](./MonsterLevelCurvePoint/Level.md) in the live data; the upper bound of the level band if it is one (row +4). |
| [ScaledValue](MonsterLevelCurvePoint/ScaledValue.md) { get; set; } | The `float32` scaled value (row +8) — climbs to `100` across the tier's level span. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [MonsterLevelCurvePoint.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/MonsterLevelCurvePoint.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
