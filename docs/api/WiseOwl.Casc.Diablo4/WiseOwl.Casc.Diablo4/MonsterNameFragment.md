# MonsterNameFragment structure

One entry of the Diablo IV `MonsterNames` registry (FR-C35, CL-105) — a localized name fragment the game composes into an elite/special monster's displayed name (e.g. `FrozenSuffix004` → `"Frostburn"`, `ElectricLanceSuffix001` → `"Boltrend"`).

```csharp
public struct MonsterNameFragment : IEquatable<MonsterNameFragment>
```

| parameter | description |
| --- | --- |
| Token | The registry token / StringList label (e.g. `"FrozenSuffix004"`). Stable across builds; the composition key. |
| Text | The localized display fragment (e.g. `"Frostburn"`). |
| Kind | Whether the token is a Prefix or Suffix (classified from the token name; the game composes a full name from a base + prefix/suffix fragments). |

## Public Members

| name | description |
| --- | --- |
| [MonsterNameFragment](MonsterNameFragment/MonsterNameFragment.md)(…) | One entry of the Diablo IV `MonsterNames` registry (FR-C35, CL-105) — a localized name fragment the game composes into an elite/special monster's displayed name (e.g. `FrozenSuffix004` → `"Frostburn"`, `ElectricLanceSuffix001` → `"Boltrend"`). |
| [Kind](MonsterNameFragment/Kind.md) { get; set; } | Whether the token is a Prefix or Suffix (classified from the token name; the game composes a full name from a base + prefix/suffix fragments). |
| [Text](MonsterNameFragment/Text.md) { get; set; } | The localized display fragment (e.g. `"Frostburn"`). |
| [Token](MonsterNameFragment/Token.md) { get; set; } | The registry token / StringList label (e.g. `"FrozenSuffix004"`). Stable across builds; the composition key. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [MonsterNameFragment.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/MonsterNameFragment.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
