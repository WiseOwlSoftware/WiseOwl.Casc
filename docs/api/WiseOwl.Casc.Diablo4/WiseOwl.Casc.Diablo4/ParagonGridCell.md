# ParagonGridCell structure

A grid coordinate on a paragon board's authored layout — row-major from the top-left (row `0` = top, col `0` = left). Paired with a [`ParagonNodeInfo`](./ParagonNodeInfo.md) by `Catalog.GetBoardNodes(int)` to describe placement on the board.

```csharp
public struct ParagonGridCell : IEquatable<ParagonGridCell>
```

| parameter | description |
| --- | --- |
| Row | Row index (`0` = top); always in `[0, board.Width)`. |
| Col | Column index (`0` = left); always in `[0, board.Width)`. |

## Public Members

| name | description |
| --- | --- |
| [ParagonGridCell](ParagonGridCell/ParagonGridCell.md)(…) | A grid coordinate on a paragon board's authored layout — row-major from the top-left (row `0` = top, col `0` = left). Paired with a [`ParagonNodeInfo`](./ParagonNodeInfo.md) by `Catalog.GetBoardNodes(int)` to describe placement on the board. |
| [Col](ParagonGridCell/Col.md) { get; set; } | Column index (`0` = left); always in `[0, board.Width)`. |
| [Row](ParagonGridCell/Row.md) { get; set; } | Row index (`0` = top); always in `[0, board.Width)`. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [ParagonGridCell.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/ParagonGridCell.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
