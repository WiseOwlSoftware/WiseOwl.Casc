# Catalog.GetParagonTooltipChrome method

FR-C23 (Option A, CL-77) — the engine's authored paragon-node tooltip chrome (per-rarity 9-slice TiledStyle panels). The consumer renders each via the existing [`ReadTiledStyle`](../Diablo4Storage/ReadTiledStyle.md) / [`TryGet`](./TryGet.md) path. See [`ParagonTooltipChrome`](../ParagonTooltipChrome.md) for the surfaced fields. The wider layout / per-state recipe (slot rects, typography, state binding) is tracked on `casc-fr#38` (FR-C26) — the FR-C7-shaped multi-CL RE thread.

```csharp
public ParagonTooltipChrome GetParagonTooltipChrome()
```

## Remarks

The eight `TooltipBackgroundRarity_*` SNOs are resolved once via [`TryGetId`](../CoreToc/TryGetId.md) + the canonical CoreTOC name; missing SNOs are dropped (an honest partial — every paragon-relevant rarity is populated on the live install, but a future build dropping one wouldn't crash the surface, just shrink the dictionary).

## See Also

* record [ParagonTooltipChrome](../ParagonTooltipChrome.md)
* class [Catalog](../Catalog.md)
* namespace [WiseOwl.Casc.Diablo4](../../WiseOwl.Casc.Diablo4.md)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
