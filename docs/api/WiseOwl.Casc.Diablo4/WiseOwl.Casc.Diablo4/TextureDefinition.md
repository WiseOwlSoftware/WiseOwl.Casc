# TextureDefinition record

A parsed Diablo IV `TextureDefinition`: pixel format, base dimensions, the per-mip `serTex` table, and the atlas `ptFrame` sub-rects. The pixel payload itself is fetched separately (it is addressable by SNO id); this metadata only lives in the combined bundle (see [`CombinedTextureMeta`](./CombinedTextureMeta.md)).

```csharp
public record TextureDefinition
```

## Public Members

| name | description |
| --- | --- |
| [TextureDefinition](TextureDefinition/TextureDefinition.md)(…) | A parsed Diablo IV `TextureDefinition`: pixel format, base dimensions, the per-mip `serTex` table, and the atlas `ptFrame` sub-rects. The pixel payload itself is fetched separately (it is addressable by SNO id); this metadata only lives in the combined bundle (see [`CombinedTextureMeta`](./CombinedTextureMeta.md)). |
| static [ParseFromBundle](TextureDefinition/ParseFromBundle.md)(…) | Parse a `TextureDefinition` from a `0x44CF00F5` combined-meta descriptor. Unlike a standalone SNO payload, the `DT_VARIABLEARRAY` descriptor here is `{ i32 pad, i32 dataOffset@+4, i32 dataSize@+8, i32 pad }` and `dataOffset` is blob-relative (measured from *descStart*), not payload-relative. Scalars are payload-base-relative with base `descStart + 4`. |
| [Codec](TextureDefinition/Codec.md) { get; } | The decoded codec for [`Format`](./TextureDefinition/Format.md). |
| [Format](TextureDefinition/Format.md) { get; set; } | Raw `eTexFormat` selector value. |
| [Frames](TextureDefinition/Frames.md) { get; set; } | Atlas sub-rects, keyed by image handle. |
| [Height](TextureDefinition/Height.md) { get; set; } | Base mip height in pixels. |
| [MipMax](TextureDefinition/MipMax.md) { get; set; } | Last valid mip level. |
| [MipMin](TextureDefinition/MipMin.md) { get; set; } | First valid mip level. |
| [SerTex](TextureDefinition/SerTex.md) { get; set; } | Per-mip data locators (index 0 = largest mip). |
| [Width](TextureDefinition/Width.md) { get; set; } | Base mip width in pixels. |

## See Also

* namespace [WiseOwl.Casc.Diablo4](../WiseOwl.Casc.Diablo4.md)
* [TextureDefinition.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/TextureDefinition.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.Diablo4.dll -->
