# Blte class

Decoder for the BLTE container ("Block Table Encoded"), the chunked codec wrapping every stored CASC blob. A blob is a `'BLTE'` magic, an optional chunk table, then one or more chunks; each chunk's first byte is its mode: `'N'` raw, `'Z'` zlib, `'F'` recursive BLTE, `'E'` encrypted (Salsa20/ARC4).

```csharp
public static class Blte
```

## Public Members

| name | description |
| --- | --- |
| static [Decode](Blte/Decode.md)(…) | Decode a BLTE blob (already stripped of any CASC archive envelope) into its logical bytes. |
| static [DecodeToStream](Blte/DecodeToStream.md)(…) | Decode a blob and expose it as a forward-only stream. |

## Remarks

Clean-room from the public TACT documentation, cross-checked against the upstream record. Decodes eagerly into a single buffer — correct and simple; CASC blobs are individually small-to-medium. Encrypted chunks require a TACT key this library does not ship, and raise [`CascEncryptedContentException`](../WiseOwl.Casc/CascEncryptedContentException.md).

## See Also

* namespace [WiseOwl.Casc.Encoding](../WiseOwl.Casc.md)
* [Blte.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/Encoding/Blte.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.dll -->
