# TvfsManifest class

A parsed TVFS (TACT Virtual File System) manifest: the path→content tree newer Blizzard titles (including Diablo IV) use instead of a classic root. Walking it yields, for every file, the lookup3 path-hash mapped to the encoding key the storage is addressed by.

```csharp
public sealed class TvfsManifest
```

## Public Members

| name | description |
| --- | --- |
| static [Parse](TvfsManifest/Parse.md)(…) | Parse a TVFS manifest tree. |
| [Count](TvfsManifest/Count.md) { get; } | Number of indexed files. |
| [Entries](TvfsManifest/Entries.md) { get; } | All path-hash → encoding-key entries. |
| [Stats](TvfsManifest/Stats.md) { get; } | Walk diagnostics (sub-manifest descent counts, a capped sample of reconstructed path strings) — for understanding the tree, not part of normal use. |
| [TryResolve](TvfsManifest/TryResolve.md)(…) | Resolve a CASC path (e.g. `Base\CoreTOC.dat`) to its encoding key. (2 methods) |
| class [TvfsStats](TvfsManifest.TvfsStats.md) | TVFS walk diagnostics. |

## Remarks

Clean-room implementation of the documented TVFS layout: a `TVFS`-magic directory header sizing three tables (path, VFS, CFT), then a recursively-walked path table. Each path node carries pre/post separator flags, a name, and either a folder span (recurse) or a node value indexing the VFS table; a VFS span points into the CFT table where the file's encoding key lives. Some entries are themselves nested TVFS directories (the build config's `vfs-1…vfs-N`); those are resolved through a caller-supplied sub-manifest reader.

This implementation indexes the first span's encoding key per file — sufficient to open the single-span data/meta/payload files game modules need.

## See Also

* namespace [WiseOwl.Casc.Tvfs](../WiseOwl.Casc.md)
* [TvfsManifest.cs](https://github.com/WiseOwlSoftware/WiseOwl.Casc/blob/main/Tvfs/TvfsManifest.cs)

<!-- DO NOT EDIT: generated by xmldocmd for WiseOwl.Casc.dll -->
